# Antworld 路线图 · P2 系列

> 规划基线: P1.9(physarum 三触角 + 真实感包 + 觅食超时返巢/路径信任, 2.9 ms/step @5000 蚁)。
> 用户方向选择: 行为生态 + 场景交互 + 规模性能 + 氛围观感, 全做, 按依赖排序。

## 总原则(贯穿红线, 沿袭 P1.x 文化)

1. 一切新机制走 schema 参数门控, **默认 0/off = 旧行为 bit 级不变**; 开新基线必须 perf_check 重录。
2. 每阶段完结写 METRICS.md: 机制、A/B 数据(同种子)、checksum、观感截图。
3. 指标与语义同读: del/index 在行程结构变化后不可跨机制比较(P1.7/P1.9 两次教训)。
4. headless 工具同步长进: 新行为必须有 dead_site_check 式的 A/B 验收脚本。

## 阶段计划

### P2.0 · 工程地基(0.5 天) ⚠ 先做
项目**没有 git**(最大工程风险)。建仓库 + .gitignore(node_modules/dist/bench-out/screenshots) +
初始 commit 打 tag `P1.9`, commit message 附现行校验和。此后每阶段一 tag。
- 验收: `git log` 可回溯; 任意阶段可 `git stash` 试验新机制。
- 顺带: README 三段话(怎么跑/面板怎么玩/指标去哪看)。

### P2.1 · 障碍与绕行(2-3 天) — 行为生态核心, 迷宫的前提
- 障碍格网(复用 field 的 gridCell 分辨率): 传感器采样点进墙 → 沿墙转向 + 位移禁止入墙;
  信息素不可沉积在墙上、扩散不穿墙(扩散核遇墙格置 0)。
- 交互: 左键工具模式切换(放食物/画墙/擦墙), 巢与食物不可放墙内。
- bench 新指标: 绕行效率 = 实际首次送达路径长 / 欧氏距离; 衔接 P1.6-P1.8 断口擦除语义(活体版)。
- 风险: 传感器贴墙抖动(沿墙滑行需切向分量), 预留 K_wall 参数调。
- 验收: 迷宫图里蚁群 60s 内找到出口食物; 绕行效率 < 2(不傻撞墙)。

### P2.2 · 报警信息素 + 捕食者(2 天) — 第二信息素通道
- field 单通道 → 双通道(trail + alarm): alarm 蒸发率独立(更快), 沉积源 = 惊逃/死亡蚂蚁。
- 捕食者实体: 用户放置(工具模式), 半径内捕杀; 蚁群涌现绕开高 alarm 浓区 → 危险消退后路线恢复。
- 顺为将来留位: "食物气味"第三通道(食物被动挥发, 供远距离引诱)。
- 风险: 双通道场显存×2、渲染混合层; 先只渲染叠加淡红色。
- 验收: 放捕食者在必经之路 → 3 分钟内主流路线绕行; 移除捕食者 → 路线 5 分钟内恢复。

### P2.3 · 天气与昼夜 ✅ 已完成(2026-09-05, tag P2.3) — 按 ANT_BIOLOGY §三 的修正版实现
- 实际落地: 内源钟与光照**分离**(dayPhase 可反转=昼行/夜行物种), 响应锐度 dayCurve=3,
  温度窗口 10–45°C 硬门控, 雨前低压抢收(dP/dt 触发), 雨/风=场衰减指数加速器(wash),
  巢内滞留按生物钟走表, 雨丝/色温滤镜, HUD 时刻+气温+活性, R/N 快捷键。
- 验收 15/15(weather_check.mjs): 恒等回归含"天气开但恒等构造逐位不变"; 反相 38× 且 rho −0.93。
- 机制细节、四条教训(假家/走表/占空比杀死 Pearson/抢收天花板)与基线重定见 METRICS.md P2.3。

### P2.3.1 · 光污染治理 ✅ 已完成(2026-09-05, tag P2.3.1) — 用户报告: 走廊烧成一片白, 读不出结构
- 病根三条(互相独立): ①色阶在 peak 处硬钳制 + 上面还叠一层 1.6 倍白热核心 → 通道冲到 2.06 被帧缓冲截断成
  纯白, 觅食网整片同色; ②WebGL/Canvas2D/PNG 三条路径各写一套色阶常数(增益 1.6 vs 1.8), 验收图不代表玩家画面;
  ③蚂蚁按 additive 混合, 5000 只压在走廊上叠成一层蓝雾。
- 落地: 新增 `render/palette.js` 作为**唯一色阶定义**(tone 曲线 + 两张 stop 表 + `glslRamp()` 生成 GLSL),
  三路径共享; `tone()` 半亮点以下线性、其上 8 个倍频程对数上肩、每通道 ≤0.99(白热段绕「青白」站避免穿灰绿);
  蚂蚁改 alpha 混合; 新参数 `toneMap`(默认 1; 0=旧硬钳制+旧 additive); `peak` 语义由「最亮」改「半亮点」0.7→0.35。
- 顺手修两个前置 bug: P2.3 引入的 6 处 `uniform vec3 uAmbient;;` 让 WebGL2 静默编译失败整整一个阶段,
  而 Canvas2D 兜底因画布已被占用必然黑屏 → 现在兜底先换一块干净画布, HUD 第一行自报渲染后端。
- 验收 25/25(glare_check.mjs, 只量像素): 纯白 5.71%→0.00%, 溢出 5.80%→0%, 网内可辨亮度级数 1→134,
  相对反差 0%→28.2%, 顶格浓度跨度 7.42→2.23 个倍频程。WebGL 实拍 A/B: 高亮像素 1.093%→0.009%。
  sim 校验和逐位不变; `toneMap=0` 的 PNG 与 HEAD 输出 SHA256 相同。教训15(渲染层必须自证)见 METRICS.md。

### P2.3.2 · 光污染治理 II ✅ 已完成(2026-09-05, tag P2.3.2) — 用户复报: 外面还裹着一大坨蓝雾, 路反而看不出来
- 病根两条(互相独立, 两条都治; 本阶段完成时间在 P2.4 之后):
  ①**sim 层** 稳态扩散长度 ℓ=sqrt(D/λ)=43.5u 是触角 26u 的 **1.67 倍** → 云比鼻子大, 多出来那圈浓度没有任何
  接收者, 却既进画面又把转向带偏; ②**渲染层** 曝光钉死在绝对浓度 0.35 上, 而富场蚁脚中位剂量是它的 135 倍 →
  走廊被压在色阶顶端, 对数肩把"雾比路暗 8 倍"画成"雾和路一样亮"。**任何固定 peak 都只能救一边。**
- 落地: `diffuseWeight` 出厂 0.06→**0.02**(由 ℓ≤sensorDist 解出 dw≤0.0215, 是推导值不是手感值);
  新增 `render/exposure.js` + 参数 `autoPeak`(默认 1, 0=固定曝光且画面逐位不变): 参考浓度锚在**蚁脚剂量中位数×GAIN=0.5**,
  只收光不加光(max 滑杆值), 收光 1.5s/给光 6s, 1/8 倍频程死区, 对数域一阶滤波。开销中位 0.019ms/帧=60fps 预算的 0.11%。
- 生物学依据: 昆虫触角感受神经元有**外周适应**(dynamic range 按背景浓度整体平移, 编码相对差)——这正是 config 里
  K_sat 存在的理由; 既然蚂蚁的鼻子会跟着剂量平移, 替蚂蚁看这张俯视图的屏幕没理由钉死在绝对浓度上。另: 足迹标记是
  断续点串, "路"的连续性来自上千次踏步而非单个标记外扩 → 用扩散糊连续带=让场替蚂蚁回答它们根本没问的问题。
- 新量具两个: `fog_check.mjs`(场层 F1-F6)、`glow_check.mjs`(像素层 G1-G6; 分箱按离路格数, 与 peak 无关所以换曝光仍可比)。
- 实测(管饱昼间 240s 三种子均值): 雾可见半径 **14.7→2.3 格**(触角只够 3.25 格), 雾亮比 ρ **0.65→0.09**,
  像素死光 **49.8%→8.2%**; 吞吐**变好**(卸货 1.071×, 超时+弃货 0.821×)——场不再拿蚂蚁读不到的梯度投票。
- 门控: `autoPeak=0` 两个场景 PNG **逐字节**= P2.4; 更强的是第三条——出厂配置(autoPeak=1 + dw 显式钉回 0.06)出的图
  也逐字节 = P2.4 ⇒ 常规玩法主画面一个字节没动。sim 层 `weather_check` 新增恒等跑(d) 把 dw 钉回 0.06 复现 P2.4 基线。
- 诚实记录十二条 FAIL / 坏判据 / 作废 / 撤销(含我自己造的假绿判据、G5 收光的真实代价、⑤d 那笔推不掉的账)见 METRICS.md §6。

### P2.3.3 / P2.3.4 · 路线成熟度门 `K_route` + 侧抑制背景减除 `lateralK` ✅ 已完成(2026-09-05) — 只在 METRICS 立账
- 两条都是 P2.3.2 的续账: 前者还掉"从没被验证过的私人路线一提交就拿满权威"那条 ⭐ 债(出厂 `K_route`=2),
  后者把显示量从"这一格有多少分子"换成"这一格比触角够得着的那一圈强多少"(出厂 `lateralK`=0.5, 纯渲染层)。
  数据、判据与更正见 **METRICS P2.3.3 / P2.3.4**; 生物学引用在本轮被逐条抓回 NCBI 重核(旧引用是编的, 已作废)。

### P2.3.5 · 画面外观: 白纸 · 墨色走廊 · 黑壳工蚁 · 会被啃的食物 ✅ 已完成(2026-09-05, tag `P2.3.5`) — 用户点名
- 用户原话即验收标准: "蚂蚁全都是蓝色像素斑点, 至少弄成黑色的、有触须的那种; 背景可以换成白色;
  食物弄真实一点, 蚂蚁搬运后食物上面应该有缺少。" 四条**全部兑付**, 且是纯渲染层 ⇒ 四钉逐位不变。
- 三条决定: ①从**加光**改**反射**(出射色 = 环境光 ×(纸色×(1−墨覆盖) + 墨色×墨覆盖)), 夜/雨与自适应曝光、
  侧抑制全部原样复用(它们改的是"哪一格多浓", 不是颜色); ②蚁是**有形状的虫体**(三段+腹柄细腰+膝状触角,
  负重=嘴里叼一粒粮而不是全身泛金, `antLen` 是屏幕像素, LOD 由体长**和蚁数**共同决定=填充率护栏);
  ③食物是**可被消耗之物**(整体缩小 + 缺口角跨度 ∝ 已吃掉比例 + 角向啃痕), `f=1−amount/a0` 只给渲染层读。
- 新模块 `render/look.js` = 形状/色表/GLSL 生成的**唯一登记处**: 三条渲染路径(WebGL2 / Canvas2D / PNG)同源,
  漂移在结构上不可能(A14/A14b 钉这条)。参数 85→**91**, 新夹子「画面外观 (P2.3.5)」6 支, 出厂全开、都能关到 0。
- 新门禁 `node look_check.mjs` **58 PASS / 0 FAIL**: 除了像素与几何判据, 还有**没有 GPU 也能跑**的 GLSL 静态 lint
  (A11-A14)与场 quad 面积判据(A16)——本轮抓到的两个真 bug 正是这一类: 四参 `max()` + 漏声明 varying 让 WebGL2
  **又**静默兜底了一次; 而场 quad 只有 4 个顶点、draw 画 6 个 ⇒ **世界只有左上那一半被画上信息素层**,
  加光时代完全隐身(clearColor 与零信号同为近黑), 换白纸才现形。Az1-Az4 四条对照臂保证判据自己会红。
- ⚠ 遗留观感差距(未修, 已记 METRICS §4): 放大时腹部 sheen 是一整块扁平浅棕椭圆, 视觉重量压过身体。

### P2.4 · 个体路线记忆(3 天) ✅ 已完成 — 从"只会跟随"到"记得路线"
- 生物学依据(见 docs/ANT_BIOLOGY.md 对照表行 6 与 §二): 真实蚁是**信息素 × 个体路线记忆双通道**,
  L. niger 开放场沿线保真 87.4%, 同种能凭个体记忆学会单向路线, 且"对信息素位置的记忆"可覆盖当前场信息。
  此前模型里蚂蚁没有个体记忆, 走廊一旦被冲散就从零重踩——这是对真实系统最大的一处过度简化。
- 落地: 每蚁 32 个航点(memA 重放 / memB 记录 双缓冲 SoA, 5000 蚁共 2.6 MB);
  **双通道份额制**——闻得到集体走廊就走路, 闻不到才掏记忆(`memShare` 按可靠度分份额, 强场下记忆让位 100.6%)。
- 诚实交底(卖点被自己证伪): **"黎明火箭"不存在**。第一版单种子 1.39x 是挑樱桃, 第二版人工抹场读出
  6720x/1.443x 是拿噪声当结论; 真相是夜里走廊确实蒸发(黎明场峰只有当日平台的 1/98~1/156), 但群体
  约 27s 就从零踩回, 真空期太短兑换不出吞吐。正常玩法总账 **1.011x**。
- 出厂 `K_mem=2` 是**真实性决定, 不是性能决定**(代价 ms/step 2.627→2.850, +8.5%)。
- 唯一站得住的正结果: 捕食者撕碎走廊后的**重建期**——走廊恢复度 73.7%→103.7%(三种子 3/3 单调),
  吞吐回升 34%→50%(2 胜 1 平); 以及弱场场景 ⑤B 的 **2.39x**。
- 验收 23 断言全绿(memory_check.mjs); 校验和重定为 ants `8297548.1091679782`, `K_mem=0` 逐位复现旧值。
  数据与教训 16-22 见 METRICS.md。

### P2.4b · 场景预设 + 统计曲线 + 跟随镜头(2-3 天) — 交互层 ✅ 已完成(2026-09-05, tag `P2.4b`)
验收兑现: 五预设一键加载(maze/breakpoint 也在一起验)、跟拍讲出完整故事(浏览器亲眼读到事件时间线)。
新门禁 `stats_check`(30 断言) + `preset_check`(25 断言); **sim 一个字没改 ⇒ 四钉逐位不变**。
如实记的未验项: `V` 录像端到端、预设下拉手工点选(只验了 `?preset=` URL)。详见 METRICS P2.4b。
- 预设下拉: 默认走廊 / 迷宫(P2.1) / 双源竞争 / 断口剂量 / 饥荒生存(依赖 P2.5, 先占位)。
- 统计 sparkline: 卸货率/负重数/空手返巢 随时间的曲线(最近 60s 滑窗)。
- 跟随镜头: 点选蚂蚁 → 相机跟随 + 轨迹面包屑 + 个体事件时间线(出发/发现/到家/迷路/超时返巢/死亡)。
- 录像导出: MediaRecorder 抓 canvas → webm, 一键保存。
- 验收: 四个预设一键加载; 跟拍一只蚂蚁能讲出它的完整故事。

### P2.4c · 调试 UI 密度 + 倍速性能(用户点名 2026-09-05) ✅ 已完成 — 见 METRICS P2.4c / HANDOVER §8
- UI: 短 `label` + `desc` 降级 hover + 分组表搬进 `core/config.js` 并加载即自检(漏登记即 throw, 删掉静默
  fallback) + 默认只展开「场景与视图」。**信息一个字没少, 只是不再常驻。**
- 倍速: `loop.js` 重写(预算闸 + 积压上限 + 按倍速分档降渲染) + `tps` 改用未钳位时间 + `inspector.resize()`
  先比尺寸。新门禁 `pace_check` 28/0；**四钉逐位不变**。
- ⚠ 上一轮那句"64× 顶格即整段时间被丢弃"**是错的**(窗口 4.0 s > 64× 的 3.2 s), 真门槛 ≥80×; 已由 T2f 钉住。
- ⚠ 交底: 本改**不买吞吐**(上限 = 1/单步成本), 买的是不卡死的镜头与诚实读数。浏览器实扫未跑成(tab 被节流)。

### P2.4d · 调试密度 II + 倍速性能 II(用户复点名 2026-09-06) ✅ 已完成 — 见 METRICS P2.4d / HANDOVER §8
- 密度: HUD 出厂只剩 **2 行**(level 0), `H` 三档循环; 面板 13 个夹子出厂只开 1 个(可见行 11);
  新**过滤框**按 名/说明/key 匹配并实时报 `N/91`(实测 `雨` = 7/91, 只自动展开「天气 / 昼夜」);
  删 `backdrop-filter`; 面板可滚。**空串完全复原**含进入过滤前的展开状态。
- 快捷键与输入控件打架是真 bug: 框里打 `f` 会切工具 ⇒ keydown 加输入守卫; 补 `/` 聚焦过滤框、
  `Esc` 两段退出(先清词再交还焦点)。浏览器闭环取证见 METRICS P2.4d §1.5。
- 倍速: 新立 `frame_check`(16/0) + `pace_check` 扩到 32/0; **画质 Q** 三档手动降采样(100/75/55, 写回
  `?renderScale=` 进分享链接)。**四钉逐位不变**, 产物 269.42 kB / gzip 81.74 kB。
- ⚠ **三个倍速假设被自家量具否证**(浏览器 A/B, 先关掉背景标签页): `stepBudgetMs=80` 只降 fps 不买吞吐;
  `renderScale=0.55` 一步没省(出画 JS 只占 tick 的 1-2%); 而 64× 的天花板 = **4.87×**(5000 蚁单核)。
  实测 **ms/step ≈ 0.163 + 0.000587×蚁数** ⇒ 16× 需 ≤1500 蚁、64× 需 ≤160 蚁。见 §P2.6 与 METRICS P2.4d §2/§3。

### P2.4e · 出厂散粮的剂量标定(用户复点名 2026-09-06: 「食物弄的真实一点, 搬运后应该有缺少」) ✅ 已完成 — 见 METRICS P2.4e
⚠ **本节那个「每蚁 15 单位 = 75,000」已被下一节 P2.4f 否证并重标**(它只盯住了最慢那一颗种子)。四版返工的读数照登。
上一批(P2.3.5)把种子画对了, 但**默认场景让它来不及被看见**: 那块食源只有 200 单位, 出厂 5000 蚁
几十秒搬空 ⇒ 缺口还没长出来图就空了, HUD 在倍速下常年读「负重 0」。本轮补的是场景剂量, 零 sim 自由度。
- 默认场景改成**两块种子**(近籽先见底的完整生命周期 + 主源撑住整场观察), 布局搬进
  `core/presets.js: buildDefaultFoods` 由 app 与门禁同源; 剂量按蚁数标定(每蚁 15 单位 ⇒ 出厂 5000 蚁 75,000 单位)。
  手点那粒从硬编码 120 单位改成与近籽同一条律(P1k)。
- 为什么是两块不是三块 / 为什么常数是这个数: 全部由新量具 `food_drain_probe.mjs` 实测定案,
  四版标定(1.05→6.3→45→24→15/蚁)的每一次返工与读数都记账(判据没有为了变绿放宽过一次)。
- 新立判据 P1d-P1l(份额定义 / 标定算术 / 总量随蚁数 / 主源占大头 / 界内不压巢盘 /
  **每块源的一趟短于负重泄压阀** / 真跑 300 秒主源仍 ≥70% / 近籽在窗口内见底 / 手点同源), `preset_check` 25→36/0。
- 顺带抓到并修掉两个自己写的哑哨兵: `world.nestRadius` 这个属性**不存在**(World 只记 nestX/nestY),
  于是「不许压巢盘」那句一直在跟 `undefined` 比大小——app 侧与判据侧各一份, 两边都钉。
- ⚠ **本轮量出来一条新的群体动力学, 没修**: 蚁群在「全力搬运」与「几乎全停」之间自激振荡
  (吞吐 0.09↔137 u/s、负重 4↔3210, 周期约 180 秒), 一粒种子见底之后整群要瞎摸 100+ 秒才重新找到下一粒。
  它是旧场景就有的行为(不是本次引入), 但它决定了剂量必须按**平均**吞吐标定; 机制归因进 P2.7 ①。

### P2.4f · 出厂散粮的**第二次**标定(2026-09-06, 用户复点名与 P2.4e 同一句: 「搬运后食物上面应该有缺少」) ✅ 已完成 — 见 METRICS P2.4f
P2.4e 那个「每蚁 15 单位 = 75,000」被**它自己的门禁**否证了: 三颗种子实测, 最忙的两颗在 **500~700 秒**
就把 75,000 单位全吃光 ⇒ README 那句「外推约 50 分钟见底」是错的, 而「主源被啃出缺口」在观察窗里根本来不及看清。
门禁之所以一直绿, 是因为 **P1i 当时只跑了一颗种子**(三颗 300 秒时主源剩 62.4%/65.6%/80.3%, 阈值 70% 恰好只放过
在跑的那唯一一颗 `drain`)。⚠ 别把它叫「最慢那颗」: `drain` 只是**在 0~300 秒这一窗**吞吐最低(走廊起得晚),
走廊成型后它反超, 新剂量下 1,215 秒吃光全部 146,750 单位。「慢」与「测量窗内还没开始干活」在 300 秒这头重合、
在见底那头完全相反——这就是教训 (59) 本轮被自己修正的部分
⇒ **判据挑种子 = 判据没在判任何东西**。
- **改法一(标定基准)**: 全程**平均**吞吐(0.0144 单位/秒/蚁) → **饱和期实测上界**(0.030)。平均把最该负责的成熟段
  摊平了, 于是窗口必然缩水一半(教训 (58))。本轮再补一刀: 跨种子**饱和期**吞吐其实只差 ~1.5 倍(132~198 u/s),
   倒是**全程平均**能差 6 倍以上(45~135 u/s)——上一批把这两个数说反了(记成「成熟期差 7 倍」)
- **改法二(一根弦 → 两根弦)**: 两块源各给各的常数——近籽 **1.35/蚁**(6,750: 管「快」, 100~150 秒演完「整粒→缺口→消失」)
  + 主源 **28/蚁**(140,000: 管「久」, 28 ÷ 0.030 = 933s ≥ 900s; 0.030 不是严格上界而是实测平台段的**下沿**
   (三颗饱和期 123~198 u/s), 判它的是实测兑现: 三颗分别见底于 1,000 / 1,095 / 1,215 秒, 都过 900 秒目标。`FOOD_UNITS_PER_ANT`
  退化成两块之和 = 29.35, 手点那粒仍与近籽同源(`handFoodDose`)。出厂总剂量 75,000 → **146,750**。
- **判据变更全部交底**(三条, 见 METRICS P2.4f §3): P1d 从「份额」改钉「每块源自己的常数」; P1e 换标定基准并把判据对象
  钉死为主源; `FOOD_OBS_MIN` 语义从「整群口粮 600 秒」改成「**主源**被啃 900 秒」; **P1i/P1l 覆盖面 1→3 颗种子,
  阈值 70% / 50% / 10% 一字未动**。
- 零 sim 自由度: 随机流仍只消耗两次 `r()` ⇒ 四钉逐位不变。顺带修掉上一批两处**文档事故**(HANDOVER 表里 P2.4d 行被
  覆写、落位读数来自随机流顺序反了的探针), 并把「探针必须与门禁共用 harness」记成教训 (57)。

### P2.5 · 能量与生死(3 天) — 从演示变生态 ✅ 已完成(2026-09-05, tag `P2.5`)——但下面那条验收**未达成**
- 能量: 空手活动耗能、到巢从储备进食回能、能量归零死亡(尸体短暂留 alarm? 交接 P2.2)、
  储备高于阈值产新蚁(数量动态)。蚁群第一次真正"为生存而觅食"。
- 默认 survivalMode=off 保基准; 开启后 HUD 种群/储备双曲线。
- 风险: 数值平衡(耗能率×食物量×蚁数); 用 bench 调参, 目标: 默认场景蚁群能自我维持。
- 验收: 断粮 5 分钟种群开始缩员, 补粮后恢复; 饥荒预设下蚁群会战略性放弃远源(P1.9 信任机制直接复用)。
兑现情况: 前者**达成**(`survival_check` T3c 首降@18s / T4 2000→1952→2000); 后者**未达成, 而且方向是反的**——
实测蚁群放弃的是**近源**(DOSE=1500 臂: 近源还剩 36% 时远源份额已经 97.8%)。原因是可推导的:
沉积按**负重时长**计 ⇒ 3.4 倍长的线被截获 3.4 倍 ⇒ 集体通道按「线的长度」而非「收益率」分配流量;
而 P1.9 那套信任/扑空机制里没有任何一条规则说「这条线更亏所以别走」。⇒ 真正欠的是**需求侧门控**(P2.7)。

### P2.6 · GPU 规模化(1-2 周, 压轴) ✅ 已完成测量(2026-09-05, tag `P2.6`)——**原方案被自己的量具否证, 5 万蚁 60fps 未达成**
- **为什么最后**: 行为集在 P2.1-P2.5 还会变, 先迁 GPU = 每个行为都要再写一遍 shader。
- ~~第一步: 信息素场上 GPU(ping-pong FBO: 扩散/衰减/沉积), 蚂蚁留 CPU —— JS 侧 2 万蚁无压力,
  场计算是网格数的大头。~~ **这句前提被否证**: `scale_dashboard` ① 实测目标规模(5 万蚁)下**场只占单步成本
  0.6–2.5%**; 场只有在「蚁少格多」时才是大头(5 千蚁 + 500×325 = 21.3%)。⇒ 这一步**不做**, 成本全在 colony 侧。
  **P2.4d 在出厂规模(5000 蚁 / 250×163)又钉了一遍**: 场 **0.1731 ms = 5.1%** vs 蚁
  **2.8120 ms = 82.1%** ⇒ 把场做到零成本只买到 1.05× 步率。**杠杆自始至终在 `colony.step`**,
  谁再提「先 GPU 场」先拿这两个数说话(METRICS P2.4d §2)。
- 实际动工的唯一一处: `sim/fields.js` 寻址重写(8 次双取模 → 一次条件回绕), **位级安全**(20 万坐标逐位对照
  0 个不同 + 四钉逐位), 跨进程配对 A/B 省 3.7%。方法学三条见 METRICS P2.6。
- 第二步(按需): 蚂蚁 GPU 化。⚠ 每蚁独立 RNG 流的 GPU 等价实现是难点, 且 GPU 模式天然放弃
  bit 级复现 → 在 METRICS 单立统计等价基准(分布/吞吐对齐, 非逐位对齐)。
- 目标: 5 万只 60fps; LOD/视锥剔除按需加。
- 验收: 1080p 下 5 万蚁 + 500×325 场 60fps(GTX 级核显亦可)。 **未达成**: 实测 sim 单步 27.4 ms = 0.61× 实时
  ⇒ **只差 1.55–1.64 倍**(旧说法「差 4.4 倍」是拿作废读数算的)。原语可归项 46% ⇒ 抢干净上限也只有 1.87 倍,
  且微基准不可外推(隔离循环说慢 11%, 跨进程生产读数说快 3.7%)。**⚠ 而这一章只测了 sim 单步, 渲染侧一个数都没量**
  ⇒ 冲量连同路线选择一起进 P2.7。

### P2.7 · 需求侧门控 + 规模化路线(新立 2026-09-05, **两条都必须先写判据后动工**)
- ① **需求侧门控**: 交哺拒绝(饿了才接受口粮) + return-rate 招募(回巢频率决定招募强度)。
  这是 P2.5 暴露的真缺口: 入库 1218.2 单位全群只吃掉 51.1(4.2%) ⇒ 现在的模型会「采到见底为止」而不是「够吃就停」。
  预登记判据至少要有: 双源剂量臂下远源份额随饥荒下降(**专为这条判据设计可观测臂**, P2.5 那两臂都不算预登记)、
  繁殖门看全群能量状态(成年蚁在饿死时不许扩军)、巢储与出巢率的负反馈能被 bench 读出。
- ② **规模化路线选择**: 先量渲染侧(5 万蚁下 Canvas2D 逐只画 / WebGL2 VBO 上传 / HUD 与曲线每帧), 再在
  WASM / worker 分片 / LOD(远处只更新不导航) / GPU 之间按判据选。⚠ 后几类都会改轨迹 ⇒ 必须先定
  「统计等价」判据(分布/吞吐对齐, 非逐位), 不许拿 ms/step 单边说好。
  ⚠ **P2.4d 把靶子缩小了**: 82% 在 `colony.step`, 而它**内部**怎么分(感知采样 / 转向 / 结算 / 收尸)还没量过
  ⇒ 第一步是按子系统插桩的探针, 不是换后端。562 ns/蚁/步 是当前的单价。
- ③ 量具待办: `survivalMode` 的算力代价现在**没有可信数字**(开关本身改写轨迹: 5 s 窗口 +2% vs 600 s 长跑 ~2 倍)
  ⇒ 需要一次跨进程配对同一状态前缀的测量。

## 明确不做(剪枝)

- 水/火/液体模拟: 地形液体性价比低, 障碍+报警已覆盖"危险地形"需求(world.js 头注的旧愿景点名即可)。
- 多蚁种竞争: 等生态闭环(P2.5)后单独立项。
- 3D: 违背数值复现文化, 成本爆炸, 收益存疑。

## 里程碑视角

- 一周内: P2.0-P2.3 → 有迷宫、有危险、有天气的蚁群世界。
- 两周内: P2.4b → 可分享、可跟拍、可录像的完整体验(P2.4 个体记忆已完成)。
- 之后: P2.5 生态闭环 → P2.6 规模化冲量。
