# Antworld 路线图 · P2 系列

> 规划基线: P1.9(physarum 三触角 + 真实感包 + 觅食超时返巢/路径信任, 2.9 ms/step @5000 蚁)。
> 用户方向选择: 行为生态 + 场景交互 + 规模性能 + 氛围观感, 全做, 按依赖排序。

## 总原则(贯穿红线, 沿袭 P1.x 文化)

1. 一切新机制走 schema 参数门控, **默认 0/off = 旧行为 bit 级不变**; 开新基线必须 perf_check 重录。
2. 每阶段完结写 METRICS.md: 机制、A/B 数据(同种子)、checksum、观感截图。
3. 指标与语义同读: del/index 在行程结构变化后不可跨机制比较(P1.7/P1.9 两次教训)。
4. headless 工具同步长进: 新行为必须有 dead_site_check 式的 A/B 验收脚本。

## 阶段计划

### P2.0 · 工程地基(0.5 天) ⚠ 先做
项目**没有 git**(最大工程风险)。建仓库 + .gitignore(node_modules/dist/bench-out/screenshots) +
初始 commit 打 tag `P1.9`, commit message 附现行校验和。此后每阶段一 tag。
- 验收: `git log` 可回溯; 任意阶段可 `git stash` 试验新机制。
- 顺带: README 三段话(怎么跑/面板怎么玩/指标去哪看)。

### P2.1 · 障碍与绕行(2-3 天) — 行为生态核心, 迷宫的前提
- 障碍格网(复用 field 的 gridCell 分辨率): 传感器采样点进墙 → 沿墙转向 + 位移禁止入墙;
  信息素不可沉积在墙上、扩散不穿墙(扩散核遇墙格置 0)。
- 交互: 左键工具模式切换(放食物/画墙/擦墙), 巢与食物不可放墙内。
- bench 新指标: 绕行效率 = 实际首次送达路径长 / 欧氏距离; 衔接 P1.6-P1.8 断口擦除语义(活体版)。
- 风险: 传感器贴墙抖动(沿墙滑行需切向分量), 预留 K_wall 参数调。
- 验收: 迷宫图里蚁群 60s 内找到出口食物; 绕行效率 < 2(不傻撞墙)。

### P2.2 · 报警信息素 + 捕食者(2 天) — 第二信息素通道
- field 单通道 → 双通道(trail + alarm): alarm 蒸发率独立(更快), 沉积源 = 惊逃/死亡蚂蚁。
- 捕食者实体: 用户放置(工具模式), 半径内捕杀; 蚁群涌现绕开高 alarm 浓区 → 危险消退后路线恢复。
- 顺为将来留位: "食物气味"第三通道(食物被动挥发, 供远距离引诱)。
- 风险: 双通道场显存×2、渲染混合层; 先只渲染叠加淡红色。
- 验收: 放捕食者在必经之路 → 3 分钟内主流路线绕行; 移除捕食者 → 路线 5 分钟内恢复。

### P2.3 · 天气与昼夜 ✅ 已完成(2026-09-05, tag P2.3) — 按 ANT_BIOLOGY §三 的修正版实现
- 实际落地: 内源钟与光照**分离**(dayPhase 可反转=昼行/夜行物种), 响应锐度 dayCurve=3,
  温度窗口 10–45°C 硬门控, 雨前低压抢收(dP/dt 触发), 雨/风=场衰减指数加速器(wash),
  巢内滞留按生物钟走表, 雨丝/色温滤镜, HUD 时刻+气温+活性, R/N 快捷键。
- 验收 15/15(weather_check.mjs): 恒等回归含"天气开但恒等构造逐位不变"; 反相 38× 且 rho −0.93。
- 机制细节、四条教训(假家/走表/占空比杀死 Pearson/抢收天花板)与基线重定见 METRICS.md P2.3。

### P2.3.1 · 光污染治理 ✅ 已完成(2026-09-05, tag P2.3.1) — 用户报告: 走廊烧成一片白, 读不出结构
- 病根三条(互相独立): ①色阶在 peak 处硬钳制 + 上面还叠一层 1.6 倍白热核心 → 通道冲到 2.06 被帧缓冲截断成
  纯白, 觅食网整片同色; ②WebGL/Canvas2D/PNG 三条路径各写一套色阶常数(增益 1.6 vs 1.8), 验收图不代表玩家画面;
  ③蚂蚁按 additive 混合, 5000 只压在走廊上叠成一层蓝雾。
- 落地: 新增 `render/palette.js` 作为**唯一色阶定义**(tone 曲线 + 两张 stop 表 + `glslRamp()` 生成 GLSL),
  三路径共享; `tone()` 半亮点以下线性、其上 8 个倍频程对数上肩、每通道 ≤0.99(白热段绕「青白」站避免穿灰绿);
  蚂蚁改 alpha 混合; 新参数 `toneMap`(默认 1; 0=旧硬钳制+旧 additive); `peak` 语义由「最亮」改「半亮点」0.7→0.35。
- 顺手修两个前置 bug: P2.3 引入的 6 处 `uniform vec3 uAmbient;;` 让 WebGL2 静默编译失败整整一个阶段,
  而 Canvas2D 兜底因画布已被占用必然黑屏 → 现在兜底先换一块干净画布, HUD 第一行自报渲染后端。
- 验收 25/25(glare_check.mjs, 只量像素): 纯白 5.71%→0.00%, 溢出 5.80%→0%, 网内可辨亮度级数 1→134,
  相对反差 0%→28.2%, 顶格浓度跨度 7.42→2.23 个倍频程。WebGL 实拍 A/B: 高亮像素 1.093%→0.009%。
  sim 校验和逐位不变; `toneMap=0` 的 PNG 与 HEAD 输出 SHA256 相同。教训15(渲染层必须自证)见 METRICS.md。

### P2.4 · 场景预设 + 统计曲线 + 跟随镜头(2-3 天) — 交互层
- 预设下拉: 默认走廊 / 迷宫(P2.1) / 双源竞争 / 断口剂量 / 饥荒生存(依赖 P2.5, 先占位)。
- 统计 sparkline: 卸货率/负重数/空手返巢 随时间的曲线(最近 60s 滑窗)。
- 跟随镜头: 点选蚂蚁 → 相机跟随 + 轨迹面包屑 + 个体事件时间线(出发/发现/到家/迷路/超时返巢/死亡)。
- 录像导出: MediaRecorder 抓 canvas → webm, 一键保存。
- 验收: 四个预设一键加载; 跟拍一只蚂蚁能讲出它的完整故事。

### P2.5 · 能量与生死(3 天) — 从演示变生态
- 能量: 空手活动耗能、到巢从储备进食回能、能量归零死亡(尸体短暂留 alarm? 交接 P2.2)、
  储备高于阈值产新蚁(数量动态)。蚁群第一次真正"为生存而觅食"。
- 默认 survivalMode=off 保基准; 开启后 HUD 种群/储备双曲线。
- 风险: 数值平衡(耗能率×食物量×蚁数); 用 bench 调参, 目标: 默认场景蚁群能自我维持。
- 验收: 断粮 5 分钟种群开始缩员, 补粮后恢复; 饥荒预设下蚁群会战略性放弃远源(P1.9 信任机制直接复用)。

### P2.6 · GPU 规模化(1-2 周, 压轴)
- **为什么最后**: 行为集在 P2.1-P2.5 还会变, 先迁 GPU = 每个行为都要再写一遍 shader。
- 第一步: 信息素场上 GPU(ping-pong FBO: 扩散/衰减/沉积), 蚂蚁留 CPU —— JS 侧 2 万蚁无压力,
  场计算是网格数的大头。
- 第二步(按需): 蚂蚁 GPU 化。⚠ 每蚁独立 RNG 流的 GPU 等价实现是难点, 且 GPU 模式天然放弃
  bit 级复现 → 在 METRICS 单立统计等价基准(分布/吞吐对齐, 非逐位对齐)。
- 目标: 5 万只 60fps; LOD/视锥剔除按需加。
- 验收: 1080p 下 5 万蚁 + 500×325 场 60fps(GTX 级核显亦可)。

## 明确不做(剪枝)

- 水/火/液体模拟: 地形液体性价比低, 障碍+报警已覆盖"危险地形"需求(world.js 头注的旧愿景点名即可)。
- 多蚁种竞争: 等生态闭环(P2.5)后单独立项。
- 3D: 违背数值复现文化, 成本爆炸, 收益存疑。

## 里程碑视角

- 一周内: P2.0-P2.3 → 有迷宫、有危险、有天气的蚁群世界。
- 两周内: P2.4 → 可分享、可跟拍、可录像的完整体验。
- 之后: P2.5 生态闭环 → P2.6 规模化冲量。
