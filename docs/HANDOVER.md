# Antworld 交接文档（HANDOVER）

> 交接基准：2026-09-06，P2.4d（调试密度 II + 倍速性能 II）完成后。上一批：P2.3.5（画面外观）、P2.5 + P2.6（两 tag 同一 commit）。
> 读者：接手本项目的开发者或新会话 AI。目标：10 分钟读完后能安全地继续开发。
> 配套阅读顺序：本文档 → METRICS.md（机制与数据权威）→ docs/ANT_BIOLOGY.md（真实性对照）。

---

## 1. 项目是什么、走到哪了

蚁群信息素（stigmergy）仿真：数千只蚂蚁靠轨迹信息素、路径积分与个体性格涌现觅食网络。
纯浏览器端（vanilla ES modules + WebGL2/Canvas2D + vite），无框架，唯一运行时依赖 tweakpane。

**最高准则（用户原话级）：行为要"有机真实"——程序化均匀观感=不合格；指标要诚实——
交付数字必须反映真实行程结构，发现旧数字掺假要大声作废（有先例，见 §5）。**

当前进度：P1.x（导航与真实感基础）→ P2.0（git 地基）→ P2.1（障碍绕行）→ P2.2（报警信息素+捕食者）→ P2.3（昼夜与天气）→ **P2.3.1（光污染治理，已完成）** → **P2.4（个体路线记忆，已完成）** → **P2.3.2（光污染 II：扩散长度压回触角尺度 + 自适应曝光，已完成）** → **P2.3.3（路线成熟度门 `K_route`：还掉 P2.3.2 欠的那条 ⭐ 债，已完成）** → **P2.3.4（光污染 III：侧抑制背景减除 `lateralK`=0.5，已完成）** → **P2.4b（场景预设+统计曲线+跟随镜头+录像，已完成，零 sim 自由度）** → **P2.4c（调试 UI 密度 + 倍速性能，已完成，零 sim 自由度）** → **P2.5（能量与生死，已完成：会饿死/会折寿/会产蚁；出厂 `survivalMode`=0 ⇒ 四钉逐位不变）** → **P2.3.5（画面外观：白纸 + 墨色走廊 + 黑壳工蚁 + 会被啃的食物，已完成，纯渲染层 ⇒ 四钉逐位不变）** → **P2.4d（调试密度 II + 倍速性能 II，已完成，零 sim 自由度）**。
已推 GitHub：https://github.com/Marshall-Jimmy/antworld（公开，origin/main 已跟踪，按阶段 tag）。
下一步：**P2.7**——① 需求侧门控（交哺拒绝 + return-rate 招募：P2.5 暴露的真缺口）② 规模化路线选择（1.6 倍缺口，
GPU 不是唯一解，且**渲染侧一个数都没量过**）。两条都**先写判据后动工**，立项依据在 METRICS P2.5 §6 与 P2.6 §5。
⚠ P2.4d 把靶子缩小了：出厂规模下单步成本里 **蚁 82.1% / 场 5.1%** ⇒ 规模化第一步是**给 `colony.step` 做子系统
分解探针**（感知/转向/结算各占多少），不是上 GPU；「先 GPU 场」这条路已被自家量具否证两次。
P2.5 交付：`sim/energy.js`（纯模型）+ `sim/colony.js` 结算/收尸/产蚁/身份 + 12 个新参数（`SCHEMA` 73→**85**）+
HUD/曲线/检视/跟拍全部改读**活蚁数**；新门禁 `survival_check` **32 PASS / 3 FAIL**（三条红的都有交代，不许凑绿）。
**头号发现是反的**：饥荒下蚁群放弃的是**近源**而不是远源（近源还剩 36% 时 98% 的流量已经去了远处），
因为沉积按负重时长计 ⇒ 集体通道按「线的长度」而不是「线的收益率」分配流量，而模型**缺需求侧门控**
（入库 1218 单位只吃掉 51）。机制、两臂数据与四条量具作废见 **METRICS P2.5**。
P2.6 交付：**没有做 GPU**，因为量具把它的前提否证了——目标规模下信息素场只占单步成本 **0.6–2.5%**。
`sim/fields.js` 只做了一处**位级安全**的寻址重写（20 万坐标逐位对照 0 个不同 + 四钉逐位），跨进程配对 A/B
实测 **5/5 轮新更快、省 3.7%**；`scale_dashboard.mjs` 从此是这个决策的唯一证据来源（它上一版**连加载都没成功过**，
文件头那串「实测读数」全部作废）。5 万蚁实测 **0.61× 实时 ⇒ 只差 1.55–1.64 倍**（旧说法「差 4.4 倍」作废）。
方法学三条（同进程 A/B 在测自己 / 微基准不可外推 / 归因要造负载复现）见 **METRICS P2.6**。
P2.3.5 交付：用户点名的四条观感（背景换白 / 蚁要黑要有触须 / 别再是蓝色像素斑点 / 食物要被啃出缺少）**全部兑付**，
且是**纯渲染层** ⇒ sim 唯一改动是给食物斑多存一个只读字段 `a0`，四钉逐位不变。新模块 `render/look.js` 是形状/色表/
GLSL 生成的**唯一登记处**（三条渲染路径同源）；参数 85→**91**、新夹子「画面外观 (P2.3.5)」6 支（出厂全开、都能关到 0，
全关 ⇒ PNG 图**逐字节**退回 P2.3.4，SHA 已登记）；新门禁 `look_check` **58 PASS / 0 FAIL**。
**本轮抓到的两个真 bug 都在"没有 GPU 就看不见"这一类里**：四参 `max()` + 漏声明 varying 让 WebGL2 **又**静默兜底一次
（P2.3.1 那类事故重演），而场 quad 只写 4 个顶点、draw 画 6 个 ⇒ **世界只有左上那一半被画上信息素层**——
加光时代 clearColor 与零信号同为近黑所以完全隐身，换白纸才现形。根治=静态 lint(A11-A14) + 面积判据(A16) + 四条对照臂。
P2.4d 交付：把 P2.4c 只改了一半的密度问题改完 —— **HUD 出厂只剩 2 行**（`H` 三档循环）、13 个容器只开 1 个
（可见行 11）、面板顶部新增**参数过滤框**（`/` 聚焦、实时报 `N/91`、`Esc` 两段退出、空串完全复原含折叠状态）。
修掉一个真 bug：全局快捷键不认焦点 ⇒ 在框里打 `f` 会把工具切成「食物」（现由输入守卫挡住，浏览器取证：值变 `雨f` 且 toast 为空）。
倍速侧**没有买到吞吐**，买到的是「为什么买不到」的算术：`field.step` **0.1731 ms(5.1%)** vs `colony.step`
**2.8120 ms(82.1%)** ⇒ 出厂 5000 蚁的本机天花板 **4.87×**；蚁数→倍速实测表拟合 **ms/步 ≈ 0.163 + 0.000587×蚁数**
（16× 需 ≤1500 蚁、64× 需 ≤160 蚁）。浏览器 A/B 三臂否证两个假设（`stepBudgetMs=80` 只降 fps；`renderScale=0.55`
一步没省，因为出画 JS 只占 tick 的 1-2%），并立了一条取证纪律：**量倍速前先关掉其它跑仿真的标签页**（三页同跑把
294 压成 165）。新交付的手动档 **画质 Q**（100/75/55 三档，写回 `?renderScale=` 进分享链接）。新立 `frame_check`
16/0、`pace_check` 28→**32/0**、`look_check` 58→**62/0**；**四钉逐位不变**。数据与三条教训见 **METRICS P2.4d**。
`core/config.js` 并**加载即自检**（漏登记/重复/不连续一律 throw，静默兜底被删）；`core/loop.js` 重写
(预算闸 + 积压上限 + 按倍速分档降渲染)；新立 `pace_check`(28 断言，虚拟时钟)；**四钉逐位不变**。
⚠ 一处**自我更正**：上一轮写进注释的"64× 顶格即整段时间被丢弃"是错的（窗口 4.0 s > 64× 的 3.2 s），
真凶是 ≥80× 与"一帧跑爆"；该更正已由 `pace_check` T2f 钉住。见 METRICS P2.4c §2。
P2.4b 交付：5 个场景预设一键加载（每个都带"一趟最少要多久"的下界推导）、60s 滑窗统计曲线、
点蚁跟拍 + 个体事件时间线、webm 录像；新立 `stats_check`(30 断言) 与 `preset_check`(25 断言)；
**sim 一个字没改 ⇒ 四钉逐位不变**。数据、预登记判据与三条更正见 **METRICS P2.4b**。
P2.3.4 交付：显示量从「每格有多少信息素分子」换成「这一格比触角够得着的那一圈强多少」，出厂 `lateralK=0.5`；
像素层死光 7.6%→**2.9%**、雾半径 R25 2.0→**1.3 格**（触角 3.25 格）、走廊路格存活 **100%**，代价＝常规画面均亮
0.65×（G6 红＝设计意图）。数据、判据与五条更正见 **METRICS P2.3.4**。
⚠ 同轮翻出一件比代码更要紧的事：**上一轮写进注释的生物学引用是编的**（连标成「已核实」的 PMID 都指向别的论文）
⇒ 已升为 §10 红线。

## 2. 快速上手

```bash
npm install
npm run dev        # http://localhost:5300  ← 必须是 5300：本机 Windows 保留端口段含 5173，vite 默认端口会 EACCES
npm run build      # 产物 dist/，当前 269.42 kB (gzip 81.74)（P2.3.5 为 268.44/81.25，P2.5/P2.6 为 250.76/73.63）
```

无头验证十六件套（P2.4d 起 +1 `frame_check`；改动后全绿才算交付；**已知 FAIL 必须逐条有交代**，且绝不为凑绿改判据——见 §7）：

```bash
node smoke.mjs         # 冒烟：食物发现/负重峰值/场总量
node perf_check.mjs    # 性能 + bit 级校验和（对基线，见 §6）
node walls_check.mjs   # 墙不变量 + 绕行验收（240s，三种子阈值 20）
node predator_check.mjs # 捕食者 A→B→C 三阶段验收（默认 seed=predcheck 现为 7/8：吞吐回升判据跨种子脆弱，见 §7）
node weather_check.mjs   # 昼夜/天气四组验收（17/17 PASS，约 9.3 分钟 / 556s；恒等(d)=dw 钉回 0.06 复现 P2.4，(e)=K_route 钉回 0 复现 P2.3.2）
                       # 分组跑：SUB=identity,storm,antiphase,temp node weather_check.mjs
node glare_check.mjs   # 光污染·色阶验收（26 项 25 PASS / 1 FAIL=顶格跨度 2.92 倍频程(判据 ≤2.5)，§7 有交代；约 5.5 分钟）
                       # 分组跑：SUB=rich,rich07,rich10,default,rain,alarm；SHOTS=1 出左右对比 PNG
node fog_check.mjs     # 光污染·场层（P2.3.2）：离最近蚁路 N 格的格子在发光多少——死光占比/衰减长度 ℓ/吞吐护栏 F1-F6
                       # SECS=240 TRAIL=40 SUB=glare,wxcheck,predcheck 可分组；出厂读数 F1✗(0.76×) F2-F6✓
node glow_check.mjs    # 光污染·像素层（P2.3.2）：画面按离路格数分箱读 tone() 之后的亮度 G1-G6（R25/ρ/死光/走廊级数）
                       # 同一份场数据并排量 13 种曝光方案；P2.3.4 起含「出厂+侧抑制」一行＝真上线的接线(画面与曝光锚点同源)
                       # 其中「自适应模块」一行真走 render/exposure.js；SEEDS=/SCEN=/PROF
node perception_check.mjs # P2.3.4 侧抑制·合成信号门禁（18 断言，秒级，不跑 sim）：门控零拷贝 / 环不含中心 /
                       # 环vs盒三档(③a③b④) / 半径敏感性(③c) / 滑窗≡暴力(⑤) / Float64 累加的代价(⑥)
node memory_check.mjs  # P2.4 记忆验收 25 断言（①恒等 ②稳定 ③昼夜 ④撤源 ⑤弱场；本轮全跑 2081s ≈ 35 分钟,期间有复验车道抢 CPU）
                       # 出厂读数 23 PASS / 2 FAIL：红的仍是 ③a/③b（绝对量纲判据，见 §7）；⑤ 段已随 P2.3.3 转绿
                       # 分组跑：SUB=identity,stable,abandon,weak 四段可单跑；③ 段=三种子×900s 不干，占全量耗时大头
                       # P2.3.3 起 ⑤ 段是 weak/weakB/weakC **三种子取最坏**（判据一个字没动，只是样本数变了）
                       # 改 sim 热路径时才必跑全量（含 ③）；只动渲染/参数层跑 ① 段恒等即可
node pace_check.mjs    # 循环门禁 32 断言（P2.4d +4：T5f-T5i 把「倍速天花板」的算式钉住；虚拟时钟，不 import sim，秒级）：时间账恒等 / 预算闸生效 /
                       # 渲染分档 / tps 口径 / paceText 量纲。⚠ core/loop.js 不在任何旧门禁的作用面上，
                       # 它只被这一条覆盖；倍速的浏览器实扫本轮**未跑成**（tab 被后台节流 ⇒ 读数作废，见 METRICS P2.4c §5）
node stats_check.mjs   # P2.4b 观测层门禁 30 断言（S1-S7，秒级，不跑浏览器）：采样纪律 / 事件优先级 /
                       # "出发"的新定义 / 曲线与故事读数；S7 段跑 90s 管饱剂量，读出 163.00 次/秒
node preset_check.mjs  # P2.4b 场景预设门禁 25 断言（P1-P6，约 115s）：五预设首见·首卸·卸货·弃货 +
                       # 同种子两次 buildPresetWorld 逐位相同（7866216.0274952538）
node survival_check.mjs # **P2.5 能量与生死门禁 35 断言（T1-T8，全量实测 6 分 38 秒）**：恒等四钉 / 储备质量守恒 /
                       # 断粮缩员与缓冲 / 补粮回升 / 大剂量自我维持 / 双源饥荒 / 尸痕门控 / 两条死亡通道
                       # 出厂读数 **32 PASS / 3 FAIL**：T6b T6c（剂量 260 下两段不可观测）+ T8b（管饱仍饿死 6/1000，
                       # 保留 FAIL，附归因复测 6→0）——三条的来龙去脉全在 METRICS P2.5 §4/§6，判据一字未改
                       # 分组跑：SUB=t1,t2,t3,t4,t5,t6,t7,t8 任意组合 · DOSE=<单位> 只影响 T6（DOSE=1500 才是「看得见选择」那一臂）
node look_check.mjs  # **画面外观门禁 62 断言 / 约 21 秒**（P2.4d：A6 高光那条拆成 5 条 + A14b 扩表 +2）：A1 三档全关逐字节退回出厂图(LEGACY_SHA 登记在文件头)、
                       # A2-A5 白纸/冷蓝消失/蚁体变暗的像素裁决、A6-A10 蚁体形状与「会被啃」的几何判据、
                       # A11-A14 没有 GPU 也能跑的 GLSL 静态 lint、A16 场 quad 铺满世界 + 混合约定成对、Az1-Az4 对照臂
                       # 分组跑：SUB=pixel,shape,glsl,param（pixel 组要跑 3 次 render_png，各约 8 秒）
node frame_check.mjs   # **P2.4d 渲染侧每帧成本门禁 16 断言（秒级，不跑浏览器）**：蚁/场两条上传路径的分配纪律
                       # （热路径零分配、门控后 exposure 清零）、每帧上传字节上界(F4a 257 KB)、渲染侧 CPU ≤ 仿真单步
                       # (F5 0.4226 vs 2.6515 ms)、侧抑制+曝光合计 ≤1.5 ms(F6)。⚠ F3 的相对判据已重述为绝对地板
                       # (≤5 µs)+热路径非零前提——比值在分子分母同时缩小时会假绿，交底见 METRICS P2.4d §1.4
open backend_check.html  # **后端体检页**（浏览器打开，不跑蚁群）：能力查询 + 逐对 compile/link + init()/failReason/getError
                       # 它回答的是「浏览器里跑的到底是 WebGL2 还是 Canvas2D 兜底」——两次静默兜底事故的教训
node scale_dashboard.mjs # **P2.6 成本仪表板（REPS=5 实测 3 分 39 秒，只读 sim，不落盘）**：① 规模分解（场 vs 蚁，含 survivalMode=1 一行）
                       # ② 原语预算（只当上界） ③ 跨进程配对 A/B + 可见性对照 ④ 负载对照
                       # MODE=scale|paired|budget · ONLY=target · REPS=<每臂轮数> · LOAD=<自旋线程数>
                       # ⚠ 这个文件上一版 import 路径写错、从来没加载成功过 ⇒ 它旧文件头里的读数全部作废（METRICS P2.6 §0）
```

交互速览（详见 README）：F/W/E/P 工具切换（食物/画墙/擦墙/捕食者），1/2/3/4 变速，右键平移，
**P2.4h 起控件分五角**：顶部整宽工具条（预设下拉 + 换种子/分享/起雨/昼夜 + 跟拍/曲线/录像/详略/画质）· 左上 HUD 读数 · 右上 tweakpane **只管参数** · 左下统计曲线 · 底部中 toast。「复制分享链接」带走参数+种子。P2.4b 另加 **G** 跟拍 / **M** 统计曲线 /
**V** 录 webm / **H** HUD 三档详略(出厂最简 2 行) / **Q** 出画分辨率三档(100/75/55)，**/** 聚焦面板过滤框
(框内 `Esc` 先清词再退出)，URL **?preset=** 直接加载场景、**?follow=1** 开局跟拍、**?renderScale=** 定画质档、**?zoom=20** 把相机停在「全景的 20 倍」（P2.4g：这样一条链接就能复现「蚁会随缩放变大」）。
⚠ 快捷键只认非输入控件：焦点在过滤框里时 `f/w/e/p/1-4` 都是打字，不是命令。
P2.3.5 另加画面旋钮：`?inkMode=` `?antStyle=` `?foodLook=`（三档外观开关，全 0 = 逐字节退回黑底加光）、
`?antLen=16`（体长，**世界单位** · P2.4g 起随缩放变大变小）、`?antVar=0.7`（个体差异）、`?trailInk=1.2`（走廊墨色浓淡）。
P2.4 另加 URL 参数：`?inspect=7` 指着一只蚁看它的记忆线，`?food=999999999` 管饱（做长程实验用），
非 SCHEMA 参数由 app.js 的 `PASSTHROUGH` 原样带回分享链接。

## 3. 代码地图

| 文件 | 职责 | 接手要点 |
|---|---|---|
| core/loop.js | **P2.4c** rAF 主循环：固定步长累加器 + 时间预算闸 + 按倍速分档降渲染 | **不在任何 sim 门禁的作用面上**（harness 自己写死循环），只被 `pace_check.mjs` 覆盖；文件头有量纲推导与一条自我更正。倍速档表/预算/accumCap 都在 `setSpeed()`，别在别处再算一遍 |
| core/config.js | 参数 schema（key/default/min/max/**label**/desc）+ **面板分组唯一登记处**（加载即自检）。**P2.3.5 起 91 个参数 / 11 个夹子**（「能量与生死 (P2.5)」12 个 +「画面外观 (P2.3.5)」6 个：inkMode/trailInk/antStyle/antLen/antVar/foodLook） | 一切新机制先在此注册参数，默认值=关。**两个出厂即开的例外**：`K_mem`=2（P2.4，真实性优先）、`autoPeak`=1（P2.3.2，因为 `effPeak=max(滑杆,自适应)` 结构上保证"没过曝的画面逐位不变"，它只可能在过曝处起作用）。`K_route`=2（P2.3.3 路线成熟度门）；`diffuseWeight` 出厂 0.06→**0.02** 是改旧参数的推导值，`lateralK` 出厂 **0**（P2.3.4 门控中）——理由都写在各自 desc/注释里 |
| core/rng.js | mulberry32 + seedNoise | 每蚁不相交随机流；见 §4 铁律 4 |
| core/weather.js | **P2.3** 内源钟+温度窗口+风暴调度 → 一个复用 env 槽 | sim 层只读 env 不 import 本模块；风暴用独立随机流 seed+"\|wx" |
| sim/ant.js | 纯函数 `antStep(st, field, …)` | 感知/转向/记忆/惊逃全在这；注释含机制理由，别删。**P2.4 记忆是份额制双通道**：`memGain = memShare*(1−confEff)`——闻得到集体走廊就走路，闻不到才掏记忆 |
| sim/colony.js | SoA 状态容器 + 每步编排 | 经济事件（捕杀/卸货/弃货/重生）都在这；零分配热路径。**P2.4 记忆 = memA/memB 双缓冲 SoA**（32 航点/蚁，A 重放 B 记录），重放·记录·提交·废弃四件事都在 step() 里。**P2.5 生死**：三条结构性决定在文件头（容量不扩 / 不用 alive[] 而用**循环外**的 swap-remove / 能量在 colony 侧结算所以 ant.js 零改动）；`population` 才是活蚁数（`count` 现在是容量），`uid` 永不复用 ⇒ `indexOfUid` 是跟拍唯一的身份锚；产蚁用**独立随机流** `_su`，收尾 pass 先收尸后产蚁（顺序反了会让新生儿本步什么都不走过） |
| sim/world.js | 巢/食物斑/墙格/捕食者实体 | 墙格=field 分辨率；P2.2 加了 placePredator/removePredator；**P2.3.5 食物斑多存一个 `a0`(出生量)**——只给渲染层算「已啃掉的比例 f=1−amount/a0」，**sim 一个字节都不读它** ⇒ 四钉逐位不变 |
| sim/energy.js | **P2.5** 能量与生死的**纯模型**（`ENERGY_FULL`/`WEAR_SHAPE`/`WEAR_ROLL_EVERY`/`etaFromMean`/`wearHazard`/`wearRollP`/`lifeScale`/`reserveLedger`） | 不持状态、不碰数组、不掷随机数——结算全在 colony。**标定与生物学推导全在文件头**（一趟 0.161 能量、cropFood=30 ⇒ 186×、5000 蚁需求 2.05 食物/s ⇒ storageCap 必需、workLife=1460=6.1 天×240 s）。引用只指向 `docs/ANT_BIOLOGY.md §四`，**不在此新增一手引用**（§10 红线） |
| sim/fields.js | Field 类（采样/沉积/扩散/衰减，环面寻址） | 通用类；alarmField 是第二个实例，不是新类。**`diffuseWeight` 的物理含义（P2.3.2）**：稳态扩散长度 ℓ=√(D/λ)×格边长，D=15·dw、λ=−ln(decayRate)/DT ⇒ dw=0.06→43.5u（触角 26u 的 1.67 倍）/ 0.02→25.1u（0.97 倍），临界 dw≈0.0215。改 decayRate 会连带挪这条线。**P2.6 唯一的 sim 改动在这**：`sample`/`deposit`/`depositTo` 的 8 次双取模 → 一次条件回绕 + 越界退回取模。**只改整数下标选择，浮点表达式逐字未动** ⇒ 四钉必须逐位不变，且 20 万坐标逐位对照过（越界 34974 个）；越界量上界 = 0.2×网格数这个前提也写在注释里 |
| core/stats.js | **P2.4b** 观测量具：Ring + `ColonyStats`(7 指标) + `foodTotal`/`spark`/`downsample` | 三条采样纪律各钉一条 `stats_check` 断言；`version` 是曲线失效信号 |
| core/presets.js | **P2.4b** 5 场景预设（默认走廊/迷宫/双源/断口/饥荒） | 坐标按 `worldW/worldH` 比例；`presetBaseline()`+`applyPresetParams()` 保证撤预设逐位还原；**不碰渲染参数**；`famine` 出厂带 `survivalMode:1`（P2.5 之前**故意不写这个键**——那时它还不存在，写了会被 `clamp` 静默丢掉 = 给用户一个假开关） |
| core/observe.js | **P2.4b** 个体事件 + 面包屑（跟拍故事线） | 8 类事件优先级 `died>starve>wear>drop>home>found>lost>start`；**"出发"=空手物理跨出巢盘**（旧定义=pauseT 归零 ⇒ 会造出"出发早于到家"的假故事）；`MAX_STEP_DIST=8` 瞬移判死。**P2.5 之后身份锚在 `uid` 上**：每步先 `indexOfUid` 解析下标（收尸压缩会搬动活蚁），解析不到才算死 ⇒ 旧写法会把被搬走的活蚁判死、又把它留下的那一格（已是另一只）继续当它读 |
| app.js | 场步进编排、门控、HUD、工具交互 | alarmActive() 门控 + ALARM_LINGER 在这；HUD 第一行自报渲染后端**与兜底原因**（`failReason` 拼进后端名——只说"兜底"不说为什么，等于让人自己去翻 console）；`renderFrame()` 每帧调一次 `updateExposure(field, colony, stepCount/60)`——**放在 sim 之后、绘制之前**，`reset()` 里必须 `resetExposure()` |
| render/palette.js | **P2.3.1 唯一色阶定义**：tone 曲线 + 两张 stop 表 + GLSL 生成器 | 改色阶只改这里；三条渲染路径的常数由它生成，不可能再漂移 |
| render/webgl2.js | instanced 蚂蚁 + 信息素场 + 报警辉光 + **墨色反射分支** | 双场渲染；色阶 GLSL 由 palette.js、形体与食物 GLSL 由 look.js 生成，别手写常数；toneMap=0 / inkMode=0 / antStyle=0 / foodLook=0 各保留旧原码。**三个新导出是给门禁用的**：`quadVertices()`(场/雨/报警共用的世界四边形，A16 用它核面积)、`shaderSources()`(着色器源码登记表，A11-A14 在 node 里静态 lint)、`failReason`(init() 失败原因，上 HUD) |
| render/look.js | **P2.3.5 画面外观唯一登记处**：纸/墨/几丁质色表 + 工蚁形状表(三段+腹柄+膝状触角+六足) + 被啃食物的几何 + **由这些表生成 GLSL** | 三条渲染路径(WebGL2/Canvas2D/PNG)共用同一张表 ⇒ 漂移在结构上不可能(A14/A14b 钉这条)。**改外观只改这里**，别在任何一条路径里手抄常数。文件头写了"为什么从发光改反射"与三档退回约定 |
| render/canvas2d.js | Canvas 兜底（PNG 管线另见 render_png.mjs） | 与 webgl 共用 palette.js + look.js；兜底画布必须是全新的一块（见 §5 教训15）。⚠ **兜底路径也要读外观旋钮**——上一轮 `antStyle`/`foodLook` 在 PNG 管线里是空开关，被 A5 抓到（验收图差点是假的） |
| ui/panel.js | tweakpane 面板分组（**P2.4c 密度整改**） | **P2.4h 之后只管参数**（预设下拉与九个动作按钮整体搬去 `ui/toolbar.js`）：短 `label` + `desc` 走原生 hover；出厂 11 个参数夹子 + 「操作」全折叠；`syncValues()` 仍负责把预设改掉的值显示回滑杆（`setPreset()` 归工具条）。**装不下走滚动**：面板根搬进 `#pane-host`，定位与 `overflow-y` 由宿主负责——`ui/panel.js:21` 那条 ⑤【装不下】已被实测推翻(末行真实点得到)。**分组不许在本文件枚举**——调 `groupOf()`，无登记直接 throw |
| ui/toolbar.js | **P2.4h 顶部整宽工具条**：场景段（预设下拉 / 换种子 / 分享 / 起雨 R / 昼夜 N）+ 视图段（跟拍 G / 曲线 M / 录像 V / 详略 H / 画质 Q） | **不新增行为**——每个按钮都是 `app.js` 里已有那个函数的入口，快捷键字母一字未改；激活态与角标由 app.js 在**状态变化点**显式推（`setOn`/`setBadge` 自带"值没变就不碰 DOM"短路，因为推它的调用点在主循环邻域里）。栏高由 **ResizeObserver** 量真实值发布进 CSS 变量 `--bar-h`（窄窗 flex-wrap 变高的时刻根本没有 resize）⇒ 浮层挂它下沿，"谁盖住谁"在任何视口宽度下都不可能发生 |
| ui/inspector.js | **P2.4** 单蚁解剖层：青色虚线航点链 + 空心圆目标 + 面板"记忆"行 | 指蚁用 `?inspect=N`；画在蚂蚁层之下，别让 `.tp-dfwv` 面板压住它 |
| render/perception.js | **P2.3.4** 侧抑制背景减除（**出厂 `lateralK=0.5`**）：显示量 = 浓度 −「触角半径(派生 3 格)那一圈」的均值 ×`lateralK` | 纯渲染层、零分配、**门控 = 原样返回 `field.buf` 这一个数组对象** ⇒ lateralK=0 时三条渲染路径逐字节不变。环半径是派生量 round(sensorDist/gridCell) 而不是新旋钮；「用环不用盒」和「宽度 ≥2R+1 的均匀核该归零」这两条语义的理由都在文件头 |
| render/exposure.js | **P2.3.2** 自适应曝光：蚁脚剂量中位数 → 参考浓度（对数域一阶滤波） | 只读 sim、不写、不掷随机数 ⇒ 校验和不受影响。两条硬约束写死在文件头：`effPeak=max(滑杆,自适应)`（**只收光不加光**）、纯渲染层。`autoPeak=0` 整个模块直接返回滑杆值。GAIN/ATTACK/RELEASE/DEADBAND/CAP 都在这，别在渲染后端另算 |
| ui/hud.js | **P2.4b** 三层 HUD（`H` 切档） | 每层带指纹缓存：内容没变不碰 DOM；0.15s 节流；倍速下所有时间读数走 `app.simSec`（仿真钟），不是墙钟。**P2.5 三本账只在 `survivalMode>0` 时出现**：L0 换「种群 存活/容量 · 巢储 · 出生/死亡(饿/竭)」，L2 加「入库/取食/产蚁耗/溢出」与「田外余粮 · 全群最低能量」 |
| ui/graph.js | **P2.4b** `M` 键 sparkline 面板 | 只在面板打开时画。**P2.5 的 pop/巢储两行带 `when`：条件为假时整行不占位**（生死没开时它们是两条水平直线，占 26 px 说一件用户已经知道的事） |
| ui/recorder.js | **P2.4b** webm 录像 | `captureStream(0)` + 每帧**末尾** `requestFrame()`：主画布 `preserveDrawingBuffer:false`，浏览器自动投帧必抓黑帧。⚠ V 键端到端本轮未在浏览器实跑 |

脚本：perf_check（校验和+性能）、smoke、bench（stigmergy 指标）、walls_check、predator_check、**glare_check（P2.3.1 光污染：同一份场数据新旧色阶逐像素对比）**、**fog_check（P2.3.2 场层：离蚁路多少格的格子在发光——死光/衰减长度/吞吐护栏 F1-F6）**、**glow_check（P2.3.2 像素层：画面按离路格数分箱读 tone() 之后的亮度，G1-G6；同一份场数据并排量 12 种曝光方案，其中"自适应模块"一行真走 render/exposure.js）**、
**weather_check（P2.3 四组验收）**、**weather_diag（P2.3 诊断台：MODE=phantom 查假家 / dwell 逐秒滞留构成 / wave 波形与占空比）**、
**memory_check（25 断言：P2.4 五组 + P2.3.2/①b1 与 P2.3.3/①b2 两组跨版本门控）**、
**pace_check（P2.4c 循环门禁 28 断言：注入虚拟时钟跑真 `Loop`，不 import sim。T1 档表/预算/病根②量化、T2 时间账恒等与两处更正、T3 tps 口径、T4 渲染分档、T5 `paceText` 量纲）**、
**survival_check（P2.5 生死门禁 35 断言：`SUB=t1..t8` 分组 · `DOSE=` 只影响 T6）**、**scale_dashboard（P2.6 成本仪表板：`MODE=scale|paired|budget` · `ONLY=` · `REPS=` · `LOAD=` 造负载复现归因；结论性数字全部由同一次运行实跑的数算出，文件头不写读数）**、
**lateral_probe（P2.3.4 只读原型：把 render/perception.js 接到 glow_check 的分箱量具上，先量再动渲染层；预登记判据 L1–L4 写死在文件头；`BENCH=<reps>` 量单帧显示量成本）**、**perception_check（P2.3.4 合成信号门禁 18 断言：环vs盒 / 2R+1 边界 / 门控零拷贝）**、**wipe_diag（P2.4 破案现场：V0 不干预 / V1 黎明抹场 / V2 抹场+清 misses，用来否证"黎明红利"）**、dead_site_check（死点 A/B）、probe_wall（墙穿透探针）、
diag_sat（诊断杂项）、render_png（RENDER_SECS/RENDER_OUT/PARAMS/WALL/PRED 出验收图；
P2.3 另加 WX_STORM_AT=<秒> 排雨、WX_JUMP=1 推时钟半周期、FOOD=<单位> 加大食源；
P2.4f 另加 **`SCENE=default`**（画出厂散粮两块种子而不是那块硬编码旧源；**默认不开**，因为 look_check A1-A5 钉的
就是默认臂的像素哈希）、`RENDER_SEED=<种子>`（换落位，默认 `render` 不动）、`CROP=x0,y0,x1,y1`+`SCALE`（特写：
缺口/虫体在 800×520 全景里判不出来）。出图前会打印**每块食源的余量%**——「缺口该不该看得见」用数字当场判定）。
全部支持 `PARAMS=k=v` 环境覆盖。

## 4. 架构铁律（红线，违反=返工）

1. **bit 级回归**：改热路径（ant.js/colony.js/fields.js 算术）必须过 perf_check 校验和。
   现行基线见 §6；新基线必须 METRICS 记录在案。
2. **参数门控**：新机制一律 schema 参数默认 0/off = 旧行为 bit 级不变（P1.9 起每次验证过）。
3. **零分配热路径**：colony 每蚁每步复用 `_st`/`_out` 槽对象，不 new。
4. **每蚁 RNG 流**：门控关闭的代码块**不得消耗** uniform()/gauss()——否则下游随机序列全变，
   "旧行为 bit 级不变"即告破产。惊逃块、墙块都按此写（短路条件在最前）。
5. **数值红线**：校验和路径上不可用平方距离替换 Math.hypot；field 扩散核与 ant.js 的
   算术/表达式顺序冻结。
6. **诚实指标**：deliveries/吞吐必须与行程结构同读；跨机制变更后旧数字不可直比；
   假数字一经发现，在 METRICS 里正式作废并重定基（P2.2 作废了 P1.9/P2.1 全部旧基线）。
7. **交付节奏**：每阶段 commit + tag + git push（含 --tags）；commit message 附关键校验和与验收结论。

## 5. 机制栈与决策史（一句话版，细节全在 METRICS.md）

| 阶段 | 内容 | 最重要的一课 |
|---|---|---|
| P1.8 | physarum 三触角寻路 | 三触角=Weber 律(转向角∝(左−右)/(左+右))的离散化，真实蚂蚁就这么转 |
| P1.8.5 | 真实感包（个体性格/微停顿/巢内滞留） | 程序化均匀观感的解药是个体差异 |
| P1.9 | 觅食超时返巢 + 路径信任 | del 必须与行程结构同读（顺路抓一口可制造海量小额假配送） |
| P2.1 | 障碍墙 + 三触角避让 | 逐帧掷硬币定侧会左右相消把蚂蚁钉死在墙前 → 个体惯用手 |
| P2.2 | 报警信息素 + 捕食者 + 诚实化 | 三课：①报警源只能是捕杀喷溅（惊逃也喷=恐慌云正反馈永不散）；②卸货必须纯物理判定（旧 \|h\|<巢盘 让 h≈0 壳蚁站食物上 60 次/秒永动卸货，旧基线全掺假）；③leak>0 时 h 返程中途必过零成极限环外壳（真实路径积分是累积误差非指数遗忘）→ leak 默认 0 |
| P2.3 | 内源钟昼夜 + 温度门控 + 雨前抢收 + 雨风冲刷 | 四课：①"到家"也必须纯物理判定——\|h\|<巢盘 会让参考点无界外漂成"假家"（同一把尺子实测 21.0% 的蚁把家认在野外，滞留被放大后就地在野地里"蛰巢"；MODE=phantom node weather_diag.mjs 可重跑）；②巢内滞留该按生物钟速率走表，不是卸货那一刻乘死常数；③昼夜响应必须超线性（dayCurve=3）——线性钟下活动相位占 85%，任何 Pearson 判据的天花板就是 −0.18；④饱和群体的"出巢率"榨不出 ×2，第二条腿是缩短行程（brisk 抑制触角扫描） |
| P2.3.3 | 路线成熟度门 `K_route`（出厂 2）：同一条线连续被走通验证几次才拿满记忆份额 | **未经检验的权威才是薄场真正的债**：集体通道做窄之后，`rel=K_mem*forget*onRoute` 里的 conf 是绝对浓度 ⇒ 场一薄，从没被走通过的记忆就自动继承更大的转向份额。修法不是重标权重(那叫拿判据当目标函数)，而是加一个可推导的门：门启用后 ⑤d 最坏种子 73.4%→102.5%、跨种子极差 44pp→15.5pp。三课：①**门槛=1 是空操作**(线一提交就 ≥1)，该机制的最小可分辨粒度是 2，扫描范围必须跨过它；②**单种子判据双向都骗人**——73.4% 那条"结构性退化"其实是单种子最坏值，另两种子 109.7%/117.4% 是过的，于是这条债被重新定性为"建成期行为方差失控"；③**门控有自锁上界**：K=5 比不加门还差(35.2%)，权威拿不到⇒不贴线⇒验证次数永远攒不够，任何"要 earned 才给"的机制必须证明门槛在时间窗内可达 |
| P2.3.1 | 有界对数色阶 + 三路径统一 + 蚂蚁改 alpha 混合 | 渲染层必须自证——**无 warn 不等于活着**：P2.3 的 6 处 `uniform vec3 uAmbient;;` 让 WebGL2 静默编译失败整整一个阶段，浏览器里跑的其实是 Canvas2D 兜底，而兜底又因画布已被占用必然黑屏。校验和 / weather_check / 验收 PNG 全都抓不到（PNG 管线绕开 DOM 与着色器）。根治=单一色阶来源 render/palette.js + HUD 自报后端；配套新门禁 glare_check（只量像素，25/25） |
| P2.4 | 个体路线记忆（32 航点/蚁 + 双通道份额制 + 扑空废弃） | 卖点被自己证伪：**"黎明火箭"不存在**。第一版单种子 1.39× PASS 是挑樱桃（三种子均值 0.995×）；第二版人工抹场读出 6720×/1.443× 是拿噪声当结论（在指数上升沿上取固定窗口比倍率）。真相：夜里走廊确实被蒸发（黎明场峰只有当日平台峰的 1/98~1/156），但群体约 27s 就从零踩回，真空期太短兑换不出吞吐。正常玩法下记忆**没有可宣称的吞吐红利**（总账 1.011×），可测表现是保真更高更稳、弃航更少、路线贴近几何下限；红利在**集体通道被持续摘走**时（⑤B 2.39×）和**捕食者撕碎走廊后的重建期**（走廊 73.7%→103.7%，三种子 3/3）。详见 METRICS 教训 ⑯–㉒ |
| P2.3.2 | 扩散长度压回触角尺度（`diffuseWeight` 0.06→0.02，推导值）+ 自适应曝光（`autoPeak`，色阶参考浓度跟蚁脚剂量走） | **超出感知距离的扩散 = 替蚂蚁回答它没问的问题**：dw=0.06 时信息素云的衰减长度 43.5 单位是触角（26 单位）的 1.67 倍，场里存着任何蚂蚁都读不到的信息，于是它们去追一团自己闻不见的云——把 dw 解到 ℓ≤触角，吞吐 **+7.1%**、失败行程 **−17.9%**，这是纯赚。第二条腿：光污染的主因**不在场层而在曝光锚点**（fog_before/after 肉眼几乎无差别就是证据），任何固定 peak 都只能在"常规玩法一片黑"和"富场糊成一坨"里选一边 ⇒ `effPeak=max(滑杆,蚁脚中位×0.5)`，只收光不加光。附带一课：**门禁工具自己也要被门禁**——本轮抓到 3 个量具自身的 bug（假场景、代码产生不了的配置、明细只印均值而判据吃最坏值），第三个让 ③a/③b 红得莫名其妙 |
| P2.4b | 场景预设 + 60s 滑窗曲线 + 点蚁跟拍 + webm 录像（零 sim 自由度） | 观测层也要自证：没有校验和可钉就喂**已知事件序列**；`stats_check` S3d 反向兜底「量具压根没读到东西」也会让逐位相同变成假绿 |
| P2.4c | 调试 UI 密度整改 + `core/loop.js` 重写（预算闸/积压上限/按倍速降渲染） | **「顶格会丢时间」这类结论先算量纲再下笔**（窗口 240 步=4.0 s > 64× 的 3.2 s ⇒ 那句话是错的）；倍速不买吞吐，买的是不卡死的镜头与诚实的达成率 |
| P2.5 | 能量与生死：按实际位移烧能、到巢取食、饿死/折寿两条通道、储备产蚁、新蚁先做内勤（出厂 `survivalMode`=0） | **蚁群放弃的是近源不是远源**：沉积按负重时长计 ⇒ 集体通道按「线的长度」而非「收益率」分配流量；而入库 1218 只吃掉 51 ⇒ 模型缺**需求侧门控**。判据本身也翻了一条：「先仓库空、后种群降」把种群当成了一只油箱 |
| P2.6 | 规模化=先测量后动工：`scale_dashboard` 三段 + `fields.js` 一处位级安全寻址重写 | **ROADMAP 的前提也要测**：目标规模下场只占 0.6–2.5% ⇒「第一步上 GPU 场」被否证；**同进程换实现的 A/B 在测 JIT 自己**（同一处改动 +6.0%/+7.7%/−3.3% 三个矛盾读数）⇒ 跨进程配对 + 可见性对照 |
| P2.3.4 | 侧抑制背景减除（出厂 `lateralK`=0.5，**纯渲染层**）：显示量 = 浓度 −「触角那一圈」的均值 | **上一轮的生物学引用是编的**（连标成「已核实」的 PMID 抓回来分别是文昌鱼基因组与下腔静脉超声）⇒ 引用必须逐条抓回 NCBI 核；另外「8 位可辨级数」这类随亮度带整体下移而缩小的尺子，**奖励的正是病本身** |
| P2.3.5 | 画面从**加光**改**反射**（白纸 + 墨渍走廊 + 近黑虫体 + 会被啃的种子），形状与色表搬进 `render/look.js` 单源 | **加光画面会藏掉几何错误**：场 quad 少写两个顶点 ⇒ 世界只画了左上那一半，而 clearColor 与零信号同为近黑，四个阶段没人看见；换白纸第一天就现形。同一轮还重演了一次「WebGL2 静默兜底」⇒ 自证必须到**原因**层（`failReason` 上 HUD + 没有 GPU 也能跑的 GLSL 静态 lint）。第三条：**只为出图而存在的代码路径也要有"切开关必须产生差异"的判据**（PNG 管线里 `antStyle`/`foodLook` 原本是空开关，被 A5 抓到） |
| P2.4d | 调试密度 II（HUD 出厂 2 行 + 面板过滤框 + 输入守卫）+ 倍速性能 II（归因探针 + 手动画质档） | **倍速的天花板是算术，不是工程**：出厂 5000 蚁下单步 82.1% 在蚁、5.1% 在场 ⇒ 64× 需要 3840 步/秒而本机只有 292，**再优化的上限是 4.87×**；`stepBudgetMs` 加大只降 fps 不买吞吐(V1 已 99% 占空)，`renderScale` 砍像素一步没省(出画 JS 只占 tick 1-2%)。三条教训：①量倍速前先关背景标签页(三页同跑 294→165)；②单步成本随仿真状态漂移 ⇒ 差 <12% 判无差异；③标签要缩到量具能证明的范围(`出画占`→`出画JS占`) |
| P2.4e | 出厂散粮的**剂量标定**（一近一主两块种子 · 每蚁 15 单位 = 75,000 · 布局搬进 `core/presets.js` 与门禁同源，零 sim 自由度） | **"画对了"不等于"看得见"**：P2.3.5 把种子画成会被啃的，但出厂那块 200 单位的源在 5000 蚁下**10 秒见底**，缺口来不及长。标定四版返工（1.05→6.3→45→24→15/蚁），每一版的错法都不同：v1 拿**单源爬升期**速率标**多源饱和区**（算出 890s 实测 95s）；v2 判据窗太短所以绿了不算数；v3 量出**赢家通吃**（225,000 单位、900 秒后两块源一口没动）⇒ 三块砍成两块；v4 份额给错。两条方法论：①**静态判据能证明"场景长什么样"，证明不了"场景会被怎么用"**；②新写的哨兵也要防自己哑掉（`world.nestRadius` 这个属性**不存在**，"不许压巢盘"一直在跟 `undefined` 比大小） |
| P2.4f | 出厂散粮的**第二次**标定：两块源各有一根弦（近籽 1.35/蚁 · 主源 28/蚁 · 总 146,750 · 标定基准从全程平均换成**饱和期平台速率**，零 sim 自由度） | **判据只跑一颗种子 = 判据没在判任何东西**：P2.4e 的 75,000 被自家门禁否证——两颗门禁种子实测 **510~630 秒**就把口粮吃光（README 那句「外推 50 分钟」作废），而 P1i 恰好只跑了那颗**在 300 秒这一窗读数最低**的种子所以一直绿（62.4%/65.6%/80.3% 对阈值 70%）。三条方法论：
①**平均吞吐不能标定饱和窗口**（拿全程平均反解必然把该负责的成熟段摊平，窗口缩水一半；本轮还把自己纠正了一次——
「跨种子成熟期差 7 倍」是错的，实测**饱和期只差 ~1.5 倍**（123~198 u/s）、**全程平均**反而差 6 倍以上；
「挑中最慢那颗」也不准，那颗只是**测量窗内还没开工**，1,215 秒见底时它反而是三颗里最能熬的）；②一根弦拉不动两台戏——近籽管「快」（两分钟演完生命周期）、主源管「久」（最忙也得啃 15 分钟），所以**每块源各有一个常数**。覆盖面 1→3 颗种子，阈值一字未动 |

| P2.4g | 蚁体长 `antLen` 从**屏幕贴片尺寸**改成**世界单位**（default 11→16 / max→40，三条渲染路径同一副尺子；细节档 `antLod` 反过来按 `体长×缩放` 的**屏幕实际像素**判；`antStyle=0` 的旧蓝方块 `3.0/zoom` 故意不动以保逐字节退回） | **「放大缩小都不变」不是 shader bug，是写在参数语义里的**：`uLen = antLen / zoom` 把虫子钉在镜头玻璃上。更难看的是**为什么它活了一个阶段**——浏览器一副尺子（除 zoom）、出图另一副（除硬编码 `ZOOM_REF=0.8`），两套口径互不校验，任何一张 PNG 都"看起来是对的"。⇒ 几何判据必须说"同一件东西在两个面上必须一致"，本轮的定案判据正是这条：**`antLen=32@S4` 与 `antLen=16@S8` 的像素几何逐块相同**（体长与缩放是同一乘子的两副面孔）。附带一条：判据的**取材条件**也是判据的一部分——语义一改，出厂出图从 lod1 掉到 lod0，A5 三格读数漂了而阈值一字未动，触角的像素判据随之失守（见 §7） |
| P2.4h | 控件**五层分区**返工：新 `ui/toolbar.js` 把「换世界 + 换看法」9 把按钮 + 预设下拉整体搬出参数面板；面板从此只管参数 | **浮层避让不能靠"预留宽度"赌内容尺寸**：第一版顶栏是居中卡片，于是把 HUD 宽度写成 `calc(100vw - 660px)`，实测卡片左沿 330 压 HUD 右沿 368（重叠 38 px）。改成整宽一条 + 浮层挂它下沿（`top: calc(var(--bar-h) + 8px)`，`--bar-h` 由 **ResizeObserver** 量真实栏高发布——窄窗换行时栏会变高，而换行根本没有 resize 事件）⇒ 可见四层两两求交零重叠，且重叠在结构上不可能发生。「堆」的真面目也第一次说清：不是行距，是**五种东西共用一个视觉槽位**。另结一条会话污染：**同窗口 6 个活标签各跑 5000 蚁仿真抢 GPU**，同一页 fps 从 27~35 读到 9~11 ⇒ 真实事件 + DOM 断言只能验状态机，**性能结论只认无头量具**（教训 (66)(67)） |
## 6. 现行基线（2026-09-06，P2.4h 出厂：`survivalMode=0` + `lateralK=0.5` + `K_route=2` + `diffuseWeight=0.02` + `autoPeak=1` + `K_mem=2` + `inkMode=1` + `antStyle=1` + `foodLook=1` + `renderScale=1` + `antLen=16`（**世界单位**）；参数 91 项 / **五层分区**：顶部整宽工具条 · 左上 HUD · 右上 tweakpane（只管参数）· 左下曲线 · 底部 toast；面板出厂**全部折叠**（11 个参数夹子 + 「操作」都收起，能看见的只有过滤框）、HUD 只 2 行）

- **P2.4g / P2.4h（画面尺子 + 控件分区）沿用同一组四钉**：`antLen` 语义从"屏幕贴片尺寸"改成**世界单位**（11→16，
  三条渲染路径同一副尺子；细节档 `antLod` 反过来按 `体长 × 缩放` 的屏幕实际像素判）+ UI 五层分区（新 `ui/toolbar.js`）
  ⇒ 两个自由度都在 `sim/*` 之外 ⇒ `ants 8285161.7223546822 / field 195.34836906715111 / 750 / 69 / 1474` 逐位不变。
  本轮门禁（同一棵定稿树，`logs/g24h_gate.txt`）：`look_check` **62/0**（A1 逐字节退回 SHA `75d1407413bcb129084f143142012226c7a7d2f5ea8e53e7c0cfcf78030327fd` 未变）·
  `frame_check` 16/0（F5 出画 0.4245 ms vs 单步 2.5897 ms）· `pace_check` 32/0（倍速上限 2.36×、已达 2.13×）·
  `stats_check` 30/0 · `preset_check` 40/0 · `survival_check` **32/3**（三条预登记红逐字复现，储备账误差 2.61e-7）·
  `perf_check` 四钉逐位（PERF 3.216 ms/step，按教训 (51) **不作交付判据**）· `npm run build` **272.93 kB / gzip 83.66 kB**（定稿树复跑，收尾当场）。
  三条几何判据与两条作废臂见 **METRICS P2.4g §2/§3**；A5 / A5b / A5c 三格读数漂移交底（7.572%→9.212% / 0.482→0.431 / 41.5%→60.4%，
  **阈值一字未动**）见 **METRICS P2.4g §4**。分区实测（1294×912 / DPR 1）：`#toolbar` 0,0,**1294×33** · `#hud` 10,**41**,323×47 ·
  `#pane-host` 978,**41**,306×**861** · `#graph` 10,746,236×156（默认隐藏）· `#toast` 634,890 ⇒ 可见四层两两求交**零重叠**，
  两处 `top=41` 由 `calc(var(--bar-h) + 8px)` 算出（`--bar-h` 实测 33px）。面板展开全部 `scrollH=2381 / clientH=861 / scrollTop=1520`
  且最底行「重置搜索计时」真实点击生效 ⇒ `ui/panel.js:21` 那条 ⑤【装不下】**已推翻**。快捷键 `M/H/Q`/`/`+`Esc`/`R/N/G` 逐键回归通过，
  ⚠ **`V` 故意没按**（会弹保存框）⇒ 那一格是豁免不是通过。⚠ **浏览器 fps 本轮不作判据**（同窗口 6 个活标签抢 GPU，同页读到 9~11 与 27~35），
  性能结论只认无头量具，见 METRICS P2.4h §5。
- **P2.4f（出厂散粮第二次标定）沿用同一组四钉**：改的是 `core/presets.js` 的两根弦，`sim/*` 一个字节没动、随机流仍只消耗两次 `r()` ⇒
  `ants 8285161.7223546822 / field 195.34836906715111 / 750 / 69 / 1474` 逐位不变。
  出厂散粮 = **两块种子 · 总 146,750 单位**（近籽 **1.35/蚁** = 6,750 · 主源 **28/蚁** = 140,000；seed `424242` 落位
  近籽 (1180,757) 210u/r22 + 主源 (1360,864) 419u/r32），标定基准 = **饱和期平台速率** `FOOD_RATE_SAT_PER_ANT`=0.030（三颗实测平台 123~198 u/s，取的是**下沿**，不是
  上一版的全程平均，也不是严格上界——判「久」的是 P1i 的实测，不是 28÷0.030=933s 这条算术）。
  登记处 `core/presets.js: FOOD_NEAR_PER_ANT / FOOD_MAIN_PER_ANT / DEFAULT_FOOD_SPOTS.upa / buildDefaultFoods / handFoodDose`。
  本轮门禁（定稿树复跑）：`perf_check` 四钉**逐位** · `preset_check` **40/0**（P1i/P1l 覆盖面 1→3 颗种子、阈值一字未动）·
  `look_check` 62/0 · `frame_check` 16/0 · `pace_check` 32/0 · `stats_check` 30/0 · `perception_check` 18/0 ·
  `survival_check` **32/3**（三条预登记红逐字复现，判据一字未改）· `smoke` 无回归 · build **270.32 kB / gzip 82.14 kB**。
  逐格读数见 METRICS P2.4f §0、
  三颗种子的 1800 秒枯竭曲线（主源撑 **1,000~1,215 秒**，三颗全部 ≥ 900 秒下限）与新剂量自我维持复测见 **METRICS P2.4f §2/§4**（本文件不复述曲线，避免又出现「文档比量具旧」）。
  ⚠ **P2.4e 那个 75,000 已被本轮否证**（三颗种子上 500~700 秒见底，README 的「外推 50 分钟」作废）；下一行保留原文只作历史。
- **P2.4e（出厂散粮剂量标定 · 剂量已被 P2.4f 重标）沿用同一组四钉**：改的是场景布局与剂量，`sim/*` 一个字节没动、随机流仍只消耗两次 `r()` ⇒
  `ants 8285161.7223546822 / field 195.34836906715111 / 750 / 69 / 1474` 逐位不变（PERF 3.373 ms/step，取证时同机有并发负载）。
  出厂散粮 = **两块种子 · 总 75,000 单位**（seed `424242` 落位：近籽 (1180,757) 210u/r22/6,750 + 主源 (1360,864) 419u/r32/68,250），
  ⚠ 这一行的读数**修正过一次**：原先写的 226u/452u 与坐标 (1202,752)/(1403,854) 是「先放粮、后建蚁」那条错误随机流算出来的
  （`buildDefaultFoods` 在 app.js 里排在 `new Colony` **之后**，顺序反了就换了一整条流）。距离比仍是 1:2、都仍在预算 616u 内，
  所以没有一条结论受影响；但**探针必须与门禁共用同一个 harness**，否则它量到的是另一个世界。落位以 `preset_check` P1h 为准。
  登记处 `core/presets.js: FOOD_UNITS_PER_ANT / DEFAULT_FOOD_SPOTS / tripBudget / buildDefaultFoods / handFoodDose`。
  本轮门禁：`preset_check` **36/0**（新立 P1d-P1l 共 11 条，钉的是**默认场景自己**）、新量具 `food_drain_probe.mjs`、
  `look_check` 62/0、`frame_check` 16/0、`pace_check` 32/0、`stats_check` 30/0、`perception_check` 18/0、
  `survival_check` 32/3（**刻意不复用**新场景，四钉是在单块 200u 下标定的）、`smoke` 无回归；build **270.30 kB (gzip 82.12)**。
  枯竭曲线（seed `drainprobe`）：近籽 92%@10s→1%@120s→**见底@150s**；主源 100%@270s→**77%@900s**；总剩 70.1%。
  ⚠ 量出一条**没修**的群体动力学：蚁群在「全力搬运」与「几乎全停」之间自激振荡（吞吐 0.03↔136.6 u/s、
  负重 4↔3210、周期约 180 仿真秒），旧单源臂同样有 ⇒ 非本次引入，但它决定了剂量必须按**平均**吞吐标定。见 METRICS P2.4e。
- **P2.4d（调试密度 II + 倍速性能 II）沿用同一组四钉**：一个 sim 字节都没动（改的是 HUD/面板/loop 标签/渲染分辨率档）⇒
  `ants 8285161.7223546822 / field 195.34836906715111 / 750 / 69 / 1474` 逐位不变（PERF 2.776 ms/step）。
  本轮门禁：`frame_check` **16/0**(新立)、`pace_check` **32/0**、`look_check` **62/0**、`stats_check` 30/0、
  `preset_check` 25/0、`perception_check` 18/0、`survival_check` 32/3(三条预登记红未变)、`smoke` 无回归、
  build **269.42 kB (gzip 81.74)**。倍速侧新增两条**可复测的实测表**（METRICS P2.4d §2）：
  ① 归因 `field.step 0.1731 ms(5.1%) / colony.step 2.8120 ms(82.1%) / 合计 3.4247 ms ⇒ 292 步/秒 = 4.87×`；
  ② 蚁数→倍速 `500→36.5× / 1000→23.0× / 2000→12.7× / 3000→8.8× / 5000→5.4× / 8000→3.4×`，
  拟合 **ms/步 ≈ 0.163 + 0.000587×蚁数** ⇒ 单价 **562 ns/蚁/步**、场 **4.26 ns/格/步**。
  UI 侧出厂读数：HUD 2 行、面板可见行 11、过滤 `雨` = 7/91(面板总高 481 px)、`/`→`Esc`→`h` 闭环实测。
- **P2.3.5（画面外观）沿用同一组四钉**：纯渲染层，sim 只多存一个只读 `a0` ⇒ `ants 8285161.7223546822 / field 195.34836906715111 /
  750 / 69 / 1474` 逐位不变；新门禁 `look_check` **58/0**，且 A1 把「三档全关」钉成**逐字节**退回 P2.3.4 出厂图
  （SHA `75d14074…0327fd`）；`pace_check` 28/0、`stats_check` 30/0、`preset_check` 25/0、`survival_check` 32/3、`smoke` 无回归。
  build **265.98 kB (gzip 80.35)**（P2.6 时 250.76 kB，增量主要是 `render/look.js`）。
  画面侧读数（同一次 `look_check` 当场打印）：近黑 legacy **95.4%** → fact **0.0%**、暖亮底 **93.6%**、
  冷蓝识别子 **2.86%** → **0.000%**、蚁体 footprint 亮度比 **0.482**、剩余食物面积比 **0.121**。

- **P2.5/P2.6（生死 + 规模化测量）沿用同一组四钉**：`survivalMode` 出厂 0 ⇒ colony 生死块整块短路（不读数组、不写数组、不掷随机数），
  `survival_check` T1 五条钉住「关着≡逐位复现出厂，且参数表里**没有**这个键时也一样」；`fields.js` 的寻址重写只改整数下标选择，
  另加 20 万坐标逐位对照。实测 `ants 8285161.7223546822 / field 195.34836906715111 / 750 / 69 / 1474` 逐位不变。
  build **250.76 kB (gzip 73.63)**；本轮门禁：`survival_check` 32/3、`pace_check` 28/0、`stats_check` 30/0、
  `preset_check` 25/0（含带生死的 famine）、`perception_check` 18/0、`smoke` 无回归。
- **P2.4c（UI 密度 + 倍速）沿用同一组四钉**：`sim/*`/`render/*` 未改，`perf_check` 读数逐位不变；
  新门禁 `pace_check` **28 PASS / 0 FAIL**；`stats_check` 30/0、`smoke` 无回归；build **241.66 kB (gzip 69.73)**。
  ⚠ 倍速的浏览器实扫**未跑成**（量具被后台节流证伪，见 METRICS P2.4c §5），不许拿 `fps 1` 那行当结论。
- **P2.4b（交互层）沿用同一组四钉**：`core/config.js`/`sim/*`/`render/*` 未改，本轮清场前后各跑一次
  `perf_check` 逐位相同；新门禁 `stats_check` **30 PASS / 0 FAIL**、`preset_check` **25 PASS / 0 FAIL**（191.3s）；
  `smoke` 无回归；build 237.05 kB (gzip 67.87)。
- **⚠ ms/step 自本节起不再当交付判据**：同机实测 2.9–7.8 ms/step 随负载漂移，且上一轮"污染来自我的标签页"
  的归因**已被证伪**（关掉全部 4 个标签页后读数更差、整机 CPU 96%）。历史值只在同一时段内互比。
  见 METRICS P2.4b §6.2 与教训 ㉞。
- **校验和（perf_check，3600 步 / 5000 蚁 / seed=perfseed）**：ants `8285161.7223546822` /
  field `195.34836906715111` / deliveries `750` timeouts `69` 空手返巢 `1474`。
  基线重定链：2290（掺假）→731（物理卸货）→752（leak=0）→752+1552（物理返巢，P2.3 教训11）
  →8297548（P2.4 记忆出厂）→8288491（P2.3.2 薄场出厂）→8285162（P2.3.3 成熟度门出厂）**＝P2.3.4 沿用同一组四钉**：
  侧抑制是纯渲染层 ⇒ 出厂与 `PARAMS=lateralK=0` 两跑逐位相同，且画面**逐字节**退回 P2.3.3（METRICS P2.3.4 §8）。
- **三重回退钉（P2.3.3 起：每条基线必须写清它钉在哪一组参数上，且至少两套 harness 交叉）**
  - `PARAMS=K_route=0` ⇒ `8288491.3883813173 / 195.34836906715111 / 750 / 69 / 1482` = **P2.3.2 出厂行逐位** ✓
    ；同一组数由 `weather_check` 恒等(e) 独立复现，`memory_check` ①b2 在另一段 60s 场景上读出
    `8311038.489231705` 逐位 ✓ ⇒ 门控是"新增自由度"而不是"改坏旧行为"。
  - `PARAMS=K_mem=0` ⇒ `8289087.5792887788 / 195.34836906715111 / 750 / 69 / 1456` = **P2.3.2 门控行逐位** ✓
    （`rel=0` 时门没有作用面，这条同时证明两代门互不干扰）
  - `PARAMS=diffuseWeight=0.06,K_route=0` ⇒ `8297548.1091679782 / 285.06957330616132 / 752 / 100 / 1552`
    = **P2.4 出厂行逐位** ✓ ⇒ 门在厚场上同样"关着就等于旧版本"。
  - **⚠ 用钉之前先读 METRICS §10.2**：`field` 那一列是**质量量具不是空间量具**（扩散核归一化 ⇒ 它不随
    浓度搬家而变；只由"沉积了多少、什么时候沉积"决定），而且 perfseed 这块食源在第 41 秒就被掏空，
    此后 1200 步纯粹是指数衰减 ⇒ **凡是只发生在食源耗尽之后的行为差异，field 列一概看不见**。
    本轮 `K_route=0` 与出厂的 field 逐位相同就是这个道理（8 次弃航差别全落在耗尽之后），
    **不许**把它当成"门没生效"的证据，也不许拿它替代 ants 列。
- **memory_check（25 断言 / 本轮 2081s ≈ 35 分钟）23 PASS / 2 FAIL**：①(5/5，含新钉 ①b2)②(4/4)
  ④(3/3)**⑤(5/5)**；红的仍只有 ③a/③b（绝对量纲判据，见 §7 第二条）。⑤ 段这次是 weak/weakB/weakC
  三种子取最坏：⑤d 建成 **102.5%**（门启用前 73.4%）、⑤a 压平 **2.48×**、⑤b 保真 46.1% vs 基线 3.0%。
  ③ 段出厂读数（三天，不做人工干预）：记忆组总账 **1.503×**、弃航 0.29×、达半稳定 10/10/10s、
  沿线保真 93.8%、路线长 235（几何下限 226）、离散度 5 vs 基线 11；基线那一臂与 P2.3.2 逐字相同
  ⇒ 按 P2.4 立的纪律**仍然不予宣称吞吐红利**（涨的是"下坠被止住"，不是"红利变大"）。
- **weather_check（17 断言 / 556s）17/17**：新增恒等 **(e)**；② 段读数照旧（出巢 302.7/s、雨前抢收
  1.64×、冲刷 0.31×、雨后重建 1.34×）。
- **glare_check（26 项 / 329s）25 PASS / 1 FAIL**：红的是顶格跨度 **2.92** 倍频程（判据 ≤2.5 且 ≤旧 0.6×；
  旧色阶同组 8.11 ⇒ 已从 8.11 压到 2.92 仍差最后一格）。同一判据在 peak=0.7 是 1.92 PASS、
  peak=10 是 1.36 PASS ⇒ 卡在"出厂滑杆 0.35"这一格，是 P2.3.4 要处理的方向之一。
  新色阶不变量仍成立：max channel 0.99、纯白 0.00%、溢出恒 0、常规玩法 effPeak 0.350 = 滑杆（只收光不加光）。
- **fog_check（6 项，三种子均值）5 / 1 FAIL**：死光 49.8%→**37.6%**（**0.76×**，F1 仍差一口气）、
  路光 40.2%（1.26×，不靠变暗过关）、级数 **157**、ℓ=**25.1u** ≤ 触角 26u、del **1.055×**、to+ab **0.835×**。
- **glow_check（6 项，三种子均值）**：P2.3.4 出厂行 R25 **1.3 格**、ρ **0.03**、离路死光 **2.9%**（0.08×）、走廊级数
  98（0.78×）、常规均亮 0.0022（0.50×）⇒ G1✓G2✓G3✓；G4✗（已交底坏判据）、G5✗（0.78× 与上一阶段 0.79× 同级，
  非新增红）、**G6✗＝本机制的设计意图**（G6 约束的是曝光模块「只收光不加光」，拿它判一个以拿掉共模辉光为目的的
  机制＝用「别关灯」判「换射灯」；判据一字未改，METRICS P2.3.4 §7 交底）。上一阶段那行（自适应模块）读数
  R25 2.0 / ρ 0.09 / 死光 7.6% / 级数 99。曝光天花板对照（×1/×2/×4 ⇒ 死光 5.4/4.9/4.4% 而级数 85/49/25）是这一轮的设计输入。
- **walls_check（8 项）ALL PASS**：无墙卸货 **666**（P2.3.2 是 608，只记读数不作归因）/ 有墙 **84**、
  首次发现 5.7s→31.5s、墙格 348、抽查 193239 格·次恒 0、295→300 段负重轨迹 0 穿墙。
- **predator_check（seed=predcheck）8/8**：A 320.1/s → B 74.8/s（23.4%A）→ 末 60s **192.6/s = 60%A**、
  邻域流量 1253→7、走廊 3043→3481（114%）、捕杀 2894 vs 盲蚁 202545（1.4%）、报警散尽 20s。
  **⚠ 该判据已知跨种子脆弱（§7 第三条）⇒ 只登记读数，不写"捕食者验收变好了"。**
- **smoke.mjs 无回归**：20s 内发现食物、峰值负重 **308**、仍在巢内 **0.3%**、场总量 774.5、最大单格 7.959。
- **P2.3.4 的逐字节证明（本轮新录）**：tag `P2.3.3` 工作树出厂图 `corridor.png` 与本工作树 `PARAMS=lateralK=0`
  出的 `screenshots/_b34k0.png` **SHA256 相同**（`962551B85364758F05DA142F5729A949B5CB65EBFACB5245ED8885CD4B0EC571`，
  800×520，79134 B）⇒ 关掉侧抑制，画面退回上一版本的**每一个字节**，不是「看起来一样」。
  出厂图 `screenshots/_b34fact.png` = `75D1407413BCB129084F143142012226C7A7D2F5EA8E53E7C0CFCF78030327FD`。
- 渲染层单帧成本（空载实测，`BENCH=400 node lateral_probe.mjs`）：环式 K=0.5 **0.430 ms/帧**＝2.58% 帧预算（判定
  ≤0.5 ms ✓）/ 盒式 0.188 / 纯拷贝（带宽下界）0.045 / **`lateralK=0` 门控 0.15 µs**。ms/step 那两条（3.246/3.162）
  受并发污染**不作数**——sim 逐位相同 ⇒ 差异只可能在渲染层，而它已被上面这行单独量过。
- `npm run build` **218.94 kB (gzip 60.33)**（P2.3.3 的 217.74/59.76；+1.2 kB 就是那个 172 行模块）。METRICS §10.3。
- **⚠ 日志重跑声明（2026-09-05，两次）**：`logs/` 下 18 份原始日志曾在一次误操作中被删（`logs/` 是
  gitignored，git 找不回）；起因是一个自称只做只读核对的脚本里实际写了删除调用——这条已进 §10 红线。
  因为这套 harness 是确定性的，已按历史里查实的确切命令逐份重跑、覆盖同名文件：**全部校验和与
  PASS/FAIL 判据读数逐位复现**（复现不上的一律就地更正，不静默改数）。P2.3.3 又追加了一批复验车道
  （`_mem33c/_wx33c/_fog33c/_glow33c/_walls33c/_smoke33c/_pred33c/_glare33c/_memidweak06c` 为干净
  UTF-8 版，`__*.txt` 为 PS 管道把中文打成乱码的原始记录，**两者读数逐位相同、乱码版保留不删**）。
  只有 `ms/step` 一类计时随机器负载浮动，本节与 METRICS 记的是当日负载下的读数。
  重跑清单与打点见 `logs/_rebase_status.txt`；结论没丢是因为它们**早已**写进本节与 METRICS §7/§10。
- 全部读数的原始日志名与判据解释在 **METRICS.md §10「新出厂基线与门禁总表」**；本节与它冲突时以 METRICS 为准。

## 7. 已知差距与技术债

- **（P2.4d 头号事实，不是 bug 是算术）倍速买不到、只能买蚁数**：出厂 5000 蚁在本机单核的天花板 **4.87×**，
  而 64× 需要 3840 步/秒。想跑高倍速只有降蚁数这一条路（16× ⇒ ≤1500 只，64× ⇒ ≤160 只）。
  已交付的补救是**让用户当场看见这件事**：HUD 报「达成 % + 本机上限 X×」，`画质 Q` 让人两秒内试出降分辨率有没有救
  （本机 dpr=1 的答案是没救）。**未验**：`renderScale` 在 dpr>1 / 4K 窗口上的收益、blur 的每帧代价、
  `colony.step` 那 82% 的内部分解。METRICS P2.4d §2/§3/§4。
- **（P2.4g 新账·判据覆盖面）** 出厂 `SCALE=0.4` 下 `look_check` 那三张 800×520 图走 `antLod(16×0.4=6.4)=`**0** ⇒
  **触角的像素判据没了**，只剩 A6/A7 的函数级判据（`look_check.mjs:310`/`:337`）与 METRICS P2.4g §2.2 的 span 表守着。
  修法很轻（把那一臂的 `SCALE` 提到 1.0 让体长落回 lod1），但**改判据的取材条件等于改判据**，得单独一轮做，不夹带在"零 sim 自由度"里。
- **（P2.4g 未修·独立渲染问题）** `render_png` 在 `SCALE=8` 时信息素场出现密集同心 moiré：场是按格最近邻采样的，
  出图缩放一大就与格不对齐。与本轮的蚁体尺子无关，登记为后续项。
- **（P2.4g 未量·dpr>1）** `canvas2d` 画用设备像素（`antLen × zoom × dpr`）、判细节档用 CSS 像素（`antLen × zoom`），
  全部几何判据只在 dpr=1 的机器上跑过。Retina/4K 上"屏幕实际像素"差 2~3 倍 ⇒ 触角与六足的降级点会挪，本轮无读数。
- **（P2.4g 未标定）** `antLen=16` 是**标称值不是标定值**：约束是「全景缩放下体长 ≈9.5 px（触角刚出来）+ 5000 蚁的填充率不吃掉帧预算」，
  不是从真实工蚁体长/巢道宽度反推的比例尺。要谈真实性得与 `worldW / nestRadius` 的比例尺一起立账（P2.7）。
- **（P2.4h 续欠）** `V` 录像端到端从 P2.4b 欠到今天：本轮只做到"按钮激活态由 `recorder.start()` 返回值推"，
  所以"跟拍一只蚁能讲出它的完整故事"这句话里**录像那一环不在证据链内**。
- **（P2.4h 未做）** 窄窗逐档截图（820 / 480 / 320）、触屏取证、**a11y**：工具条按钮有 `title` 与快捷键角标，
  但没有 Tab 焦点顺序与 `aria-pressed`，读屏用户目前只能靠 tooltip。窄窗只验了"顶栏会换行且 `--bar-h` 跟着长"这一条兜底存在。
- **（后续可做·未验）** 关掉多余标签后复测浏览器 fps：本轮只证明了它**不可信**（会话污染），没测出干净值应该是多少。
- **（P2.5 未达成，头号）模型缺需求侧门控 ⇒「战略性放弃远源」不仅没做到，方向还是反的**：饥荒下蚁群在**近源还剩 36%**
  的时候把 98% 的流量搬去了远源。原因是可推导的：沉积按**负重时长**计 ⇒ 3.4 倍长的线被截获 3.4 倍 ⇒
  集体通道按「线的长度」而非「线的收益率」分配流量，而 P1.9 的信任/扑空机制里没有任何一条规则说「这条线更亏所以别走」。
  另一半证据是量纲：入库 1218.2 单位全群只吃掉 51.1（4.2%）——**没有「饱了就停止采」的门**。修法=交哺拒绝 +
  return-rate 招募（P2.7 第一项，判据先写死后动工）。数据与两臂见 METRICS P2.5 §6。
- **（P2.5）繁殖门只看存粮水位，不看全群能量**：`reserve ≥ birthFill` 就招兵，于是 T6 那一臂把净入库 356 单位
  全换成 89 只新蚁，而同期 2029 只成年蚁饿死。真实饥荒是先保蚁后与已有工蚁。与上一条同属「需求侧」，一起修。
- **（P2.6）5 万蚁 @60fps 未达成**：实测 0.61× 实时 ⇒ 只差 **1.55–1.64 倍**（旧说法「差 4.4 倍」作废）。
  原语可归项 46% ⇒ 抠干净也只有 1.87 倍，且**微基准不可外推**（同一处改动：隔离循环说慢 11%，跨进程生产说快 3.7%）。
  ⚠ **本章只测了 sim 单步，渲染侧一个数都没量** ⇒「1.6 倍」很可能乐观。路线（WASM / worker 分片 / LOD / GPU）在 P2.7 选，先写判据。
- ✅**（P2.4e 已还第一条腿）默认走廊开生死必缩员**：原来出厂那块食源只有 **200 单位**，5000 只蚁分 ⇒ 平均每只 0.04。
  P2.4e 标到每蚁 15 单位 = 75,000，P2.4f 重标成**两根弦 = 146,750**（近籽 6,750 + 主源 140,000）。复测
  `survivalMode=1` 跑 700 秒，**三颗**种子全部 **5000 → 5000**：`drain` 最低 4,997 · 出生=死亡 405/405 ·
   余粮 146,750→102,653；`pinseed` 最低 4,998 · 753/753 · →101,819；`424242` 最低 4,936 · 875/875 · →104,122。
  ⚠ **三条要一起读**：① 「剂量只是分母」这条不变量在生死模式下**三颗种子全部成立**——三颗的出生/死亡与
  最低种群与 P2.4e 那次**逐字相同**、余粮差**恰好 71,750**（= 140,000 − 68,250）。上一版在这里写的是
  「第三颗分岔了，机制未查」——**那是凭记忆填表填出来的假反例**：那三个数在 `logs/*.txt` 里 `rg` 零命中，
  720 秒逐步比对探针（`logs/divergence24f.mjs`）也证明一步都没分岔。改口全过程见 METRICS P2.4f §2.2 与教训 (63)。
  （METRICS P2.4f §2.2 表 + §4）。② 「撑得住」只保端点：`424242` 那一臂 t=600s **巢储 600→57**、100 秒饿死
  530 只，之后靠 875 只出生爬回来 ⇒ 这正是上面第一条「缺需求侧门控」的现场证据，别把三颗绿读成「没有波动」。
  第二条腿（「开局就淘汰」当玩法）仍然没做，也不需要做了。
- ✅**（P2.3.3 已还，且这条债的描述本身被更正过）薄场下的双通道权威失配**：原来写的是"⑤d = 73.4%，
  本轮唯一推不掉的真代价"。**这个描述不准确**——⑤ 段当时是单种子读数，73.4% 只是**最坏的那一个种子**
  （另两种子 109.7% / 117.4% 是过的）。三种子重测之后正确的定性是：**建成期记忆的行为方差失控**
  （跨种子极差 44 pp），而不是"薄场让记忆亏 26%"。
  修法也换了：不按新场尺度重标 `memShare`/`memFail`（那是拿判据当目标函数，换个尺度还得再标一次；
  而且生物学上站不住——真实蚁的沿线保真**不随访食经验即时调制**）。改成可推导的**路线成熟度门**
  `K_route`=2：同一条线连续被走通验证几次才拿满份额，门槛用比例（`memIA ≥ memNA*0.6`，与"扑空该不该
  算在路线头上"同一个阈值）⇒ **不引入新的绝对尺度，换场不必重标**。
  兑付：⑤d 最坏 73.4%→**102.5%**、跨种子极差 44pp→**15.5pp**、⑤ 段 5/5 全绿；
  **代价如实记**：⑤a 最坏红利 2.88×→**2.48×**（门是拿红利换稳健的），且 `K_route=1` 是空操作、
  `K_route=5` 会自锁（比不加门更差）。全部扫描/三种子数据在 METRICS P2.3.3 §1–§9。
- ✅**（P2.3.4 已治）残留光污染：雾是「空间上的共模」**。前两把刀（色阶、曝光）都到天花板之后换坐标：
  `render/perception.js` 做侧抑制背景减除（显示量 = 浓度 −「触角半径那一圈」均值 × `lateralK`，出厂 0.5）。
  兑付：像素层死光 7.6%→**2.9%**、雾半径 R25 2.0→**1.3 格**、ρ 0.09→**0.03**、走廊路格存活 **100%**、
  路格八度跨度 **1.45×**、相对反差 **1.23×**（结构反而更可读）。
  **残差如实记**：①常规玩法均亮 0.65×（G6 红＝设计意图）；②凡「空间上大范围缓变的积累」（探索网、巢周老堆、
  雨后均匀场）都会变暗甚至消失——判据压不住，靠出图肉眼裁决（`screenshots/_b34fact.png` vs `_b34k0.png`：
  热核 4944 像素一个没动、蓝雾那一档 −58%）；③场层 `fog_check` 的 F1（0.76×）**本轮没重跑**，理由是它只读
  `field.buf`、不 import 渲染层，而 sim 侧已被逐位证明钉住。全部数据与五条更正见 METRICS P2.3.4。
- ✅**（P2.3.5 已治）画面是"蓝色噪点"而不是蚁群**。旧画面是黑底加光：5000 只蚁 = `vec3(0.35,0.65,1.0)`
  的冷蓝光点，叠在 95% 纯黑底上（实测近黑占比 **95.4%**、冷蓝识别子 **2.86%**）——用户那句"全都是蓝色像素斑点"。
  换成反射模型：暖白纸 + 土黄→深褐墨渍走廊 + 近黑虫体（三段+腹柄细腰+膝状触角）+ 会被啃的种子。
  兑付（`look_check` 当场读数）：近黑 **0.0%**、冷蓝 **0.000%**、暖亮底 **93.6%**、蚁体 footprint 亮度比 **0.482**、
  剩余食物面积比 **0.121**（缺口角跨度 ∝ 已吃掉比例）。三档全关 ⇒ PNG 图**逐字节**退回 P2.3.4（A1 已登记 SHA）。
  全部推导与四条否证（含两个真 bug）见 METRICS P2.3.5。
- **（P2.3.5 遗留观感差距，未修）放大看腹部 sheen 是一整块扁平浅棕椭圆**（`hr = ch + mid*0.9`，sheen 在 0.74× 半径内
  恒为 1），视觉重量压过身体。默认 11 px 下不明显 ⇒ A6 只钉"高光只在腹部"这个方向，没钉它的面积。
- **（P2.3.5 未验）位级退回只覆盖 PNG 管线**：WebGL2 / Canvas2D 两条路径的"关掉退回旧画面"只有肉眼与 A5 类比值判据
  （没有 GPU 的 CI 本来也跑不了）。另外墨色 × 夜/雨色温乘子的组合只跑过白天常亮；20000 蚁 × lod2 的填充率
  只由 A7 的档位上界间接保证，没在浏览器里实测帧时间。
- **③a 的绝对量纲阈值不可跨 dw 复用**：`逐种子最大黎明场峰 < 10` 这个"10"是 P2.4 在 dw=0.06 的场上顺手写的，
  薄场把场峰整体抬高之后它必然红（出厂读数最大 23.1）。**改成比值或分位**才是尺度不变的判据。FAIL 保留、阈值未改。
- **捕食者「吞吐回升 ≥ 25%A」判据跨种子脆弱**：本轮三种子 × 两 dw 矩阵跑下来，两栏都是 2 胜 1 负、
  FAIL 落在不同种子（0.06=12% / 0.02=19%），中位回升 46% vs 57%。⇒ P2.4 记的"8/8"是抽签，
  其三种子表本来就是 2 胜 1 平。**作废**"腿 A 造成 52%→19% 退化"这条归因（METRICS §6.7）。
  修法：扩到 5–8 种子，或改成"回升的分位数"判据。
- **报警红仍然是固定峰值**：`alarmPeak`=1.2 只管色阶，捕杀高峰期场值能堆到 64（约 5.7 个倍频程）——
  同一类毛病的另一处。下一轮把 P2.3.2 这套（锚点=脚下载量 + 只收光不加光 + 门控回滑杆）原法搬给报警通道。

- **记忆没有"正常玩法吞吐红利"**（P2.4 实测，不是缺陷而是结论）：昼夜三天总账仅 **1.011×**，因为夜里
  走廊确实被蒸发（黎明峰 32~156×，按三种子汇总最小仍有 98×；判据是比值不是绝对值），但群体
  **约 27s** 就从零踩回，真空期太短兑换不出卸货量。别把这条当"还没做好"，
  它已经被三种子否证过两次（METRICS 教训 ⑱⑳㉑）。
  **P2.3.2 更新**：dw 降到出厂值 0.02 之后，同一个 ③ 段读出总账 **1.285×**（逐种子 1.504/0.991/1.510×，
  基线自身跨种子极差从 1.2% 涨到 **43.7%**）——数字涨了，但按同一条纪律**仍然不予宣称红利**；
  而且这一段同时把 ③a/③b 两条前提判据顶红（METRICS §6.9/§6.10）。dw=0.06 的对照组读数见 METRICS §6.12。
- **记忆红利的真实兑现处已量化，但强度有限**：①集体通道被持续摘走时 ⑤B **2.39×**（基线保真 3.0% vs 记忆保真 60.5%）；②捕食者撕碎走廊后的**重建期**——三种子走廊重建 73.7%→**103.7%**（3/3 单调，可作结论），
  吞吐回升 34.0%→49.7%（2 胜 1 平，只能说"通常更好"不能说"总是更好"）。这两条是目前唯一站得住的正结果。
- **两个记忆参数未经独立标定**（是"能用"不是"最优"）：`memFail`=0.6 的扑空归因门槛与 `memForget`=2 的遗忘节拍只过了 ①②④⑤
  的合理性检验（④ 撤源后覆盖 100%→75%→**28%**，判据要求 ≤50%，余量足），**没做过扫描寻优**。
  另外 ⑤ 的"建成期也 +8.2%"是**单种子**观察，够不上结论。想动这几个数先跑 `node memory_check.mjs` 当回归网。
- **信息素衰减偏快**：trail 半衰期 ~23s，真实跨度 1 min–数天（建模惯例 ~5 min）；alarm 13.5s
  对应真实的秒–分钟量级，尚可。整体再校准前先想清楚破坏哪些既有标定。
- **K_wall=4 vs 0**：三种子方向支持避让但方差大（有墙 56~128，跨种子极差 130%），仍**不能**下统计结论。
  P2.4 的三种子复测把它从"1 种子的 111→179"降级为"不稳健"，见 METRICS 墙场景否证表。
- **即时重生=新蚁**是简化（真实卵→工 ~43 天），归 P2.5 数量动态。
- **报警渲染峰值钳制**：alarmPeak=1.2 只管色阶，高捕杀期场值可堆到 64——渲染正常，别误判成 bug。
  同理信息素场在 P2.3 的长程管饱场景可达 58。P2.3.1 之后这不再是问题：色阶改成有界对数上肩，
  `peak` 是**半亮点**而不是「最亮」，256×peak 才到白热上限，任何浓度都不会再截断成白（溢出恒为 0）。
- **一夜蒸发（P2.4 已量化并部分兑付）**：dayLength=240 时正午场峰 58.3 → 深夜 6.0（半衰期 23s vs 夜长
  ~120s），网络每夜清空、白天重新成路。K_mem=2 后"重建"从几十秒变为"到岗即有路线"，但**兑换成的吞吐
  只有 **1.011×**（dw=0.06 时代；出厂 dw=0.02 下同段 1.285×，但极差 43.7%，见 METRICS §6.12 与上面第一条）；真正吃到红利的是集体通道被持续摘走的场景（⑤B 2.39×）与捕食者重建期。
- **抢收没有"巢内预备队"**：真实 ×2–3 是从巢内任务池抽人，本模型全群皆外勤（常态 90% 已在
  巢外），出巢倍率天花板只有 ~1.6×。等 P2.5 年龄多态/任务分配补上这一维。
- **ROADMAP.md 的 P2.2 节已过时**（写的是"惊逃蚂蚁也沉积"，实际为防正反馈改为死者喷溅）；
  机制以 METRICS.md 为权威，ROADMAP 只看后续阶段。
- 报警剂量只有 panic 分支；真实的低浓度=聚集查看（剂量分级）未做（ANT_BIOLOGY §五，增强方向）。


## 8. 路线图与下一步（ROADMAP.md P2.3–P2.7 + 生物学修正）

- **✅ P2.4d 调试密度 II + 倍速性能 II：已完成（2026-09-06，tag `P2.4d`）**。密度：HUD 出厂 2 行 + 面板只开 1 个容器 +
  参数过滤框(`/` 聚焦、`N/91` 实时计数、`Esc` 两段退出、空串完全复原) + 输入守卫(修掉「框里打 f 会切工具」)。
  倍速：新立 `frame_check` + `pace_check` 天花板四钉 + **画质 Q** 手动降采样；三条浏览器 A/B(V1/V2/V3)全部**如实记为
  否证或无差异**，没有一条被包装成性能提升。零 sim 自由度 ⇒ 四钉逐位。METRICS P2.4d 全章。
- **P2.4b 场景预设 + 统计曲线 + 跟随镜头 + 录像：已完成**。四预设一键加载 + 跟拍讲出完整故事两条验收都兑现
  （浏览器亲眼读到 `跟拍 出发 38 · 发现食物 37 · 到家卸货 36 · 超时返巢 1` 与逐条时间戳）；
  ⚠ 未验项如实记：`V` 录像端到端、预设下拉的**手工点选**（只验了 `?preset=` URL 路径）。
- **✅ P2.5 能量与生死：已完成**，但 ROADMAP 那条验收「饥荒预设下蚁群会战略性放弃远源」**未达成，而且方向是反的**。
  做到的：能量按**实际位移**烧、到巢从储备取食、两条死亡通道（饿死 / 外勤折寿 Weibull k=3）、储备过 `birthFill` 才产蚁、
  新蚁先做 `broodT` 内勤、死亡留尸痕（`corpseAlarm`，远小于捕杀喷溅）、`uid` 身份让跟拍能跟到一只蚁**饿死**。
  出厂 `survivalMode=0` ⇒ 四钉逐位不变（T1 五条钉住，含「参数表里没有这个键」那一版语义）。
  标定是**解出来的**（一趟 0.161 能量 / `cropFood`=30 ⇒ 186× / 5000 蚁需求 2.05 食物/s ⇒ 必须有 `storageCap`），
  生物学只指向 `ANT_BIOLOGY §四`（不新增一手引用）。`survival_check` **32 PASS / 3 FAIL**：T6b/T6c 不可观测 +
  T8b 保留红（附归因复测 6→0）；判据自身翻掉一条（T3b「先仓库空后种群降」把种群当油箱）+ 作废四处量具假行。
  **未达成与需求侧门控**见 §7 与 METRICS P2.5 §6/§7。
- **✅ P2.6 规模化：已完成测量、已否证原方案，但 5 万蚁 @60fps 未达成**。
  ① 规模分解：目标规模下**场占 0.6–2.5%**（只有「蚁少格多」时场才是大头：5 千蚁+500×325 = 21.3%）
  ⇒ 不做「把场搬上 GPU」这一步。② 唯一动的 sim 代码是 `fields.js` 的**位级安全**寻址重写：20 万坐标逐位对照
  0 个不同 + 四钉逐位 + 跨进程配对 A/B **5/5 轮新更快、省 3.7%**（每次都跑一对「故意慢一倍」的可见性对照才敢引用）。
  ③ 原语预算：可归项 46% ⇒ 全清零上限 1.87 倍；实测 0.61× 实时 ⇒ **只差 1.55–1.64 倍**（旧说法 4.4 倍作废）
  ⇒ 规模化路线（WASM/worker/LOD/GPU）在 P2.7 里选，**渲染侧尚未量过**是最大未验项。METRICS P2.6 全章。
- **✅ P2.3.5 画面外观（用户点名）：已完成，纯渲染层 ⇒ 四钉逐位不变**。用户四条诉求（背景换白 / 蚁要黑 /
  要有触须 / 食物要被啃出缺少）全部兑付：出射色 = 环境光 ×(纸色×(1−墨覆盖) + 墨色×墨覆盖)，蚁是三段+腹柄细腰+
  膝状触角的近黑虫体（负重=嘴里叼一粒粮，不再全身泛金），食源是会被啃出扇形缺口的种子（`f=1−amount/a0` 只给渲染读）。
  形状/色表/GLSL 生成搬进 `render/look.js` **单源**（三条渲染路径不可能再各写一套常数）；参数 85→**91**、
  新夹子「画面外观 (P2.3.5)」6 支出厂全开、全关 ⇒ PNG **逐字节**退回 P2.3.4。新门禁 `look_check` **58/0**，
  含**没有 GPU 也能跑**的 GLSL 静态 lint（A11-A14）与场 quad 面积判据（A16）+ 四条对照臂（Az1-Az4）。
  两个真 bug 都属于「没 GPU 就看不见」这一类：四参 `max()` + 漏声明 varying ⇒ WebGL2 **又**静默兜底一次；
  场 quad 4 顶点 vs draw 6 ⇒ **世界只画了左上那一半**（加光时代 clearColor 与零信号同为近黑，隐身四个阶段）。
- **⏳ P2.7（新立，两条都必须先写判据后动工）**：① **需求侧门控**——交哺拒绝 + return-rate 招募，验收=在预登记的
  双源剂量臂下「远源份额随饥荒下降」，并修掉「成年蚁在饿死时还在招兵」；② **规模化路线选择**——先量渲染侧，
  再在 WASM / worker 分片 / LOD（远处只更新不导航）/ GPU 之间按判据选。另有 P2.5 §7 那条「开关本身改轨迹 ⇒
  `survivalMode` 的算力代价没有可信数字」的量具待办。
- **✅ P2.4c 调试 UI 密度（用户点名）：已完成**。三条病根（原记两条，落地时查出第三条）：
  ① label 直接吃 `desc`（73 行、平均 32.7 字、最长 114 字）⇒ 换短 `label`，`desc` 降级 hover，**信息一个字没少**；
  ② 分组表手抄在 UI 里、抄漏静默 fallback 进「世界」⇒ 搬进 `core/config.js` 并**加载即自检**
  （漏登记/重复/不连续/顺序不一致 ⇒ throw），**兜底夹子被删掉**；③ 所有夹子默认展开 ⇒ 只展开「场景与视图」。
  出厂分组实测：5/7/8/9/9/2/6/17/10 = 73 = `SCHEMA.length`。数据见 METRICS P2.4c §1。
- **✅ P2.4c 倍速性能（用户点名）：已完成，但结论与上一轮的预判不同——先把错的那句改掉**：
  ~~旧写法 64× 顶格即整段时间被丢弃~~ **是错的**。旧顶格 `maxSteps=240` = **4.0 s** 窗口，而 64× 一帧最多
  带进 `maxDt(0.05)×64` = **3.2 s** ⇒ **64× 根本碰不到顶格**；真正丢时间的门槛是 **≥80×**（T2g 实测 128× 旧写法
  丢 288 s = 17280 步，新写法 0 s）。这条更正由 `pace_check` T2f 钉住，别再被脑补回来。
  真病根是另外两条：① **一帧跑爆**（64× 一帧 192 步 × 8 ms = 1.6 s ⇒ rAF 掉到 1 fps，HUD/镜头/输入全停）
  ⇒ `stepBudgetMs` 预算闸 + `accumCap=maxDt×倍速`（比旧 4.0 s 更紧，没有放宽）+ 按倍速分档降渲染；
  ② `tps` 窗口累加了**被钳过的 dt** ⇒ "达成 %"在最吃紧时朝好的方向撒谎（旧口径高估 4.005 倍，T3b 钉住"为什么必须改"）；
  ③ `ui/inspector.js:resize()` 每帧无条件写 `cv.width/height` ⇒ 1600×900@2 每帧白扔 ~11 MB backing store，现在先比尺寸。
  ⚠ **交底**：这一改**不买吞吐**（步/秒上限 = 1/单步成本，T1f 新 128 vs 旧 142 步/秒），买的是不卡死的镜头 +
  诚实的达成率 + 1× 路径逐字节不变。5 万蚁要跑得动是 P2.6 的活。倍速的**浏览器实扫本轮未跑成**（tab 被后台节流
  ⇒ 读数作废，METRICS P2.4c §5），权威证据只有 `pace_check` 28/0。
- **P2.3.3 路线成熟度门 `K_route`：已完成**（还的是 P2.3.2 欠下的那条 ⭐ 债，不是新功能）。出厂 2；
  `?K_route=0` 逐位退回 P2.3.2。机制、扫描曲线、三种子重新定性与教训㉓㉔㉕见 METRICS P2.3.3。

- **P2.3.4 光污染 III（侧抑制背景减除）：已完成**（不在原 ROADMAP 上，是光污染这条线的第三段）。出厂
  `lateralK=0.5`：显示量 = `max(0, 浓度 − K×「触角半径那一圈」的均值)`，环半径由 `sensorDist/gridCell` 派生（3 格），
  与 dw=0.02 的信息素云衰减长度 ℓ=3.14 格撞在同一个数上。**这张图改显示「蚂蚁分得出的差异」而不是「化学计量」**，
  依据是核实过的三条：触角叶突触前侧抑制做增益控制（Olsen & Wilson 2008, Nature 452(7190):956–960）、侧抑制带来
  浓度不变性（Kim & Wang 2009, J Biol 8(1):4）、弓背蚁触角跨轨迹边缘采样（Draft et al. 2018, J Exp Biol 221）。
  ⚠ **上一轮在这里写的「Bahrey & Wilson 2004 Nature 452:246」「Draft, Burd-Field, Kain & Graham」「Galizia &
  Szyszka」三条引用全部作废**（前两条作者/年份/页码是编的，第三条查无此文）——作废过程见 METRICS P2.3.4 §2。
  `lateralK=0` 时 `perceivedField()` 原样返回 `field.buf` ⇒ 三条渲染路径逐字节不变（已用 SHA256 证明）。

- **P2.3.1 光污染治理：已完成**（不在原 ROADMAP 上，由用户报告"走廊烧成白"触发）。三条渲染路径统一到一个
  色阶来源 render/palette.js，色阶改成有界对数上肩，蚂蚁改 alpha 混合，新参数 toneMap 门控旧行为。
  顺带把哑了一个阶段的 WebGL2 修回来。细节、逐像素数据与教训15 见 METRICS P2.3.1。

- **P2.3 天气与昼夜：已完成**（按 ANT_BIOLOGY §三 的修正版实现——内源钟与光照分离、
  相位可反转、温度窗口硬门控、雨前低压抢收、雨风=场衰减指数加速器）。细节与数据看 METRICS P2.3。
  储备彩蛋：洪水蚁筏/婚飞。
- **P2.4 个体路线记忆：已完成**（信息素×个体记忆双通道，32 航点/蚁 + 份额制 + 扑空废弃）。
  **出厂默认开启 `K_mem=2` 是"真实性"决定，不是"性能"决定**——在这套参数下它快不出来（三天总账 1.011×）。
  卖点"黎明火箭"被自己证伪、两版假数字已作废。全部数据、三版演化过程与教训 ⑯–㉒ 见 METRICS P2.4。
- **P2.4b 预设+统计+跟拍+录像**：sparkline 顺手解决 §7 的 K_wall 统计化（跟拍 ?inspect=N 已提前做完）。
- **P2.5 能量与生死**：ANT_BIOLOGY §四 的核心启示——**外勤折寿**（Cataglyphis 外勤期望 6.1 天
  vs 巢内数月）是比捕食者更日常的死亡压力；年龄多态（年轻巢内→年长外勤）可接进现有任务系统；
  重生延迟化。
- **P2.6 GPU 规模化**：压轴，原因见 ROADMAP（行为集未稳前迁 GPU=每个行为重写 shader）。
  GPU 天然放弃 bit 级 → 单立统计等价基准（分布/吞吐对齐）。
- 明确不做：水/火液体、多蚁种竞争、3D（ROADMAP 剪枝节）。

## 9. 文档索引

| 文档 | 内容 | 地位 |
|---|---|---|
| README.md | 怎么跑、怎么玩、红线速记 | 入口 |
| METRICS.md | P1.6–P2.6 + P2.3.5 + P2.4d–P2.4h 全部机制、A/B 数据、校验和、几何判据、分区实测、教训(㊿ 之后用 (51)…(67) 纯数字) ①–㊿ | **机制与数据权威** |
| ROADMAP.md | P2 系列规划与剪枝 | 规划参考，个别节已过时 |
| docs/ANT_BIOLOGY.md | 真实蚂蚁生物学对照手册：14 项机制↔生物学对照表 + 四方向事实 + 参数启示 | 真实性标尺 |
| docs/HANDOVER.md | 本文档 | 交接快照 |

## 10. 协作约定（给接手者，尤其是 AI）

- 用户以短指令（"继续"）放手式推进，路线图已整体批准；但**每阶段交付必须**：门禁全绿 →
  METRICS 记录 → README/文档同步 → commit+tag+push。
- 拿不准是否"有机真实"时，先查 docs/ANT_BIOLOGY.md 有没有真实对应物；没有的机制要在
  文档里标注"工程需要"。
- 汇报克制：给结论和数字，不复述过程；交付物给 file:// 链接。
- 文档语言中文；代码注释中文（理由写进注释是本项目传统，改行为先改注释）。
- **只读就是只读**：任何自称只读核对/只读比对的脚本，代码里不允许出现写、删、覆盖落盘。本轮 18 份
  原始日志就是被一个这样命名的脚本删掉的（见 §6 的日志重跑声明）。需要落盘就在文件名和注释里写明它是写操作。
- **说"两个配置校验和相同"之前，先问这个量具看不看得见差异**。P2.3.3 的 `field` 列在
  `K_route=0/2`、`K_mem=0/2` 下都逐位相同，看着像"门没生效"，实际是它只量**沉积质量×衰减时序**：
  扩散核归一化 ⇒ 浓度搬家不改这个和；而 perfseed 那块食源第 41 秒就被掏空，此后 1200 步纯衰减 ⇒
  只发生在耗尽之后的行为差异它一概看不见（METRICS §10.2 有拆解实测）。**证据要用看得见差异的那一列。**
- **量具不许有"看起来在测其实没测"的行**：`lateral_probe` v1 有一行写着"半径覆写成 1"，但覆写没被
  下游接住 ⇒ 它打印的是上一行的复读。凡是"敏感性检查"行，必须能证明它真的改变了被测配置。
- **文档里的每一条命令都必须是查实的原文**：不许凭印象重写一遍怎么跑。本轮为此单独补了一次更正提交
  （METRICS §6 第 11 条，METRICS.md:955：`SUB=idweak,weak` 这个子集根本不存在）。写不出真实命令，就等于没有证据。

- **引用必须核实到作者/年份/卷页，而「核实」＝当场把记录抓回来读**。P2.3.4 开工时代码注释里的两条生物学引用是
  凭印象写的（作者、年份、页码、甚至中间三位作者的姓都是编的），而**上一轮的会话摘要把它们标成「已核实」**、附的
  两个 PMID 抓回来一看分别是文昌鱼基因组（18563158）与下腔静脉超声（23602752）论文。⇒ 别人（包括上一轮的我）
  说「已核实」不算数；文档里留可点链接，链接背后必须是一次真实检索。METRICS P2.3.4 §2 是这一条的完整作废记录。
- **注释也不许引用「跑过其实没跑」的证据**（上一条「量具不许有假行」的镜像）。`perception.js` 两处写着「合成用例 ④⑤
  已钉住」「实测退到 8.9e-8」，而仓库里没有这个脚本、那个数字也复现不出来。补法不是删话，是**把话变成能跑的东西**：
  `perception_check.mjs`（18 断言）。写误差必须同时给绝对值和分母——旧那个 8.9e-8 恰好是本轮实测值（3.81e-6）的
  **相对**口径，可口径没写清楚就等于不可用。
- **占比型量具有退化分母**：「死光＝杂散发光/总发光」在整幅图被抹平时是 0/0，实测能报出 13.09× 这种伪读数。
  写比值之前先问分母还可能不会动；引用时要么两侧远离 0，要么换绝对量。
- **一条消息只准发一个 REPL 调用**（`node_repl/js` / `cua_repl/js`）。并行发会把上一次调用建立的绑定
  （`cua`/`tab`）打回 `is not defined`，白跑一轮还顺带烧掉一次整棵 AX 树的输出；`timeout_ms` 一律省略
  （传数字会被序列化成字符串 → `-32602: invalid type string`）。METRICS P2.4b ㉝。
- **归因也是一种数字**：写"这个读数不作数，因为 X"必须附一次把 X 拿掉的复测。本轮把 4 个标签页全关掉之后
  ms/step 反而更差（7.0–7.8），上一轮的归因当场作废。METRICS P2.4b §6.2 / 教训 ㉞。
- **PowerShell 字符串插值**：数组元素必须写 `"$($L[$i])"`；写 `"$L[$i]"` 会把**整个数组**打印出来
  （本轮实测一次吐出 350KB 工具输出）。
- **正式量具的第一条断言是「我自己能被 import 成功」，而且它的文件头不配拥有读数**。`scale_dashboard.mjs` 的 import
  写成 `'../core/config.js'`（它在仓库根）⇒ 每一次都以 `ERR_MODULE_NOT_FOUND` 退出，而它文件头抄着一整串「实测读数」，
  下一轮果然把它们当证据抄进了摘要。规矩：读数只在 stdout；结论性数字由**同一次运行实跑的数**算出；每一段自带自证
  （③ 每次跑一对「故意慢一倍」的臂，看不见差异就整段作废）。METRICS P2.6 §0/§3。
- **在同一个进程里换实现来做 A/B，量的是 JIT 自己在重新优化**。换一次 `Field.prototype.sample` 就把已编译的
  `Colony.step` 打回解释执行，钱正好花在被测的那几十步里 ⇒ 同一处改动给出 +6.0% / +7.7% / −3.3% 三个互相矛盾的读数。
  正确做法：每臂一个独立进程 + 交替先后顺序 + 取 min + 可见性对照。附带一条：**微基准的 ns 不可加也不可外推**
  （隔离循环说慢 11%，跨进程生产读数说快 3.7%），它只配当**上界**。METRICS P2.6 §3/㊺。
- **归因也是一种数字——包括「这个读数不作数，因为机器在忙」这一种**：必须附一次把负载**造出来**的复测
  （`LOAD=4`：ms/步 +21%，3.7% 的配对差被冲平成 −0.7%）。顺带一条有用的推论：**比值型读数（场占）对负载不敏感，
  绝对 ms/step 敏感** ⇒ 要跨轮比较就尽量选比值。METRICS P2.6 §4。
- **判据要写清它约束的是谁**：G6「不许把主画面做暗」约束的是曝光模块（只准收光不许加光）。拿它判一个以「拿掉
  共模辉光」为目的的新机制，会得到一个红 + 一个正确的设计。**不改判据**（改判据＝凑绿），只在三处交底：
  量具文件头、METRICS §7、本节。
- **自证要到「原因」这一层，只说「兜底」不算自证**。P2.3.1 立了「后端名上 HUD」，四个阶段之后同一个 bug
  （着色器编译失败 ⇒ 静默退回 Canvas2D）又烧掉一小时，因为 HUD 只说「Canvas2D(兜底)」不说为什么。
  现在：`failReason` 拼进 HUD、着色器源码进 `shaderSources()` 登记表、`look_check` A11-A14 在**没有 GPU 的 node 里**
  静态 lint（实参个数 / varying 配对 / 声明完整 / 生成块真的接进了源码），配 Az 对照臂保证判据自己会红。
- **只为出图而存在的代码路径也要有「切开关必须产生差异」的判据**。P2.3.5 的验收图差点是假的：
  `antStyle` / `foodLook` 在 PNG 管线里是**空开关**（切了图不变），而浏览器路径已经改了——A5 第一次跑差异恒为 0 才暴露。
  凡是「另一条实现路径」（兜底 / 离线渲染 / 导出）都要有一条「拧这个旋钮，输出必须变」的断言。
- **墙钟吞吐读数之前，先清点还在跑仿真的标签页**。本轮三个页同时跑仿真，把出厂路径的 294 步/秒 压成 165——
  一个凭空的 44%「性能回归」。关掉其它页之后 294 与 node 单核探针 292 吻合，才确认代码没退步。
  配套一条：**单步成本会随仿真状态漂移**(走廊成形后 55 秒内 294→264)，所以本轮定死「A/B 差 <12% 一律判无差异」，
  宁可放弃结论也不认领假归因。METRICS P2.4d §3 / 教训 (51)(52)。
- **量具标签要缩到它能证明的范围**：`tickMs` 只含 JS 的一个 tick，浏览器的绘制与合成在 tick 之外 ⇒
  HUD 那个百分比从「出画占」改叫「**出画JS占**」。因此 blur / renderScale 的代价**报不进这个数**，
  只能登记成未验项交给浏览器 A/B 定案——不要为了有个数字而夸大标签。METRICS P2.4d §1.4/§4 / 教训 (53)。